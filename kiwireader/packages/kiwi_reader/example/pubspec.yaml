name: kiwi_reader_example
description: Runnable demo of the KiwiReader Read tab (read → select → highlight).
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.4.0
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  kiwi_reader:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
