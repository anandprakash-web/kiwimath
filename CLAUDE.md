# Memory

## 🚦 SHIP GATE — run before EVERY ./deploy.sh (founder rule, 2026-06-28) — protects the MOAT
- **The moat = the skill questions + the nested cluster (variant) questions + the adaptive ruleset.** No change ships until these are verified intact. Run **`./ship_gate.sh`** (repo root) → it must print **GATE GREEN**, else DO NOT DEPLOY.
- The gate checks, in order: (1) `pre_deploy_check.py` green; (2) `content_qa_scan.py` flags **did not increase** vs baseline (currently 80 known pre-existing L3–L7 dups/direction — lower the `SCANNER_BASELINE` in the script when a real fix pass reduces it); (3) **every served olympiad question still carries `skill_id` + `skill_seq` + `skill_difficulty`** (0 missing); (4) smokes `smoke_level_v3` (NO DISJOINT), `smoke_adaptive_skill` (right→next-skill, wrong→cluster-drip, **no-regress/resume**), `smoke_contest_league`, `smoke_store` — all PASS.
- **If a change touches CONTENT or tags**: also re-run `content-live/qa-reports/cluster_concepts.py` (idempotent) and require `integrity_bad=0` before the gate, so skill/nested tags + the ladder positions stay coherent. ⚠️ open follow-up: add a ladder-version stamp so saved adaptive positions don't reinterpret after a re-cluster.
- The adaptive ruleset spec lives in `content-live/qa-reports/ADAPTIVE_RULESET_2026-06-17.md`; the defect catalogue in `qa-reports/MISTAKES_REPOSITORY.md`. **Last gate run 2026-06-28: GREEN** (adaptive 8/8, level 17/17, contest 6/6, store, challenge 16/16, 0 missing tags across 19,642 Qs, scanner 80 = baseline).

## Engagement + The Climb (adaptive CAT) — 2026-06-28
- **Celebration** (`app/lib/v3/celebration.dart`): confetti burst on a correct answer (bigger for L1–L3), self-contained CustomPainter (no package), haptic; **sound OFF + mutable**, visible-celebration ON by default. Two Profile toggles (Celebrations / Sound effects, `CelebrationPrefs`). Wired in PracticePage `_submit`. Needs APK.
- **Visible streak + daily loop** (retention, the #1 problem): the app-bar flame already existed but was stateless. Added `practiced_today` to the wallet (`gamification.get_profile_summary` = `streak_last_date == _ist_date(0)`). Olympiad **hero subtitle = streak nudge** ("3-day streak — solve 1 today to keep it" / "nice! see you tomorrow" / "start your streak"). **Compete banner is now stateful** (`_CompeteBanner` in kiwi_v3.dart): LIVE-now (red dot + ring) / "Opens in Hh Mm" live countdown / "Played today", from `getContestToday` (no new contest plumbing). Backend ships via deploy; UI needs APK.
- **THE CLIMB = adaptive Challenge (GRE/GMAT-style mini-CAT), SEPARATE from the skill-ladder (the ladder stays the learn/moat surface; the Climb measures+stretches).** Reuses the existing 3PL toolkit `app/assessment/irt_model.py` (Fisher `information(θ)`, `estimate_ability_eap` → θ+SE) and the SAME 200–800 scale as Progress (`proficiency_levels.theta_to_scale_score`) → no disjoint. New `backend/app/services/challenge_service.py` (pool from each level's `irt_b`, a=1.0/c=.2; loop = EAP θ → max-info select among TOP_K=6 for exposure → fixed **10**-item stop → rating+band+peak; resume-safe session + best/history in `FirestoreBackedStore`; **measurement-only v1, no economy writes**) + `backend/app/api/challenge.py` (`/v3/challenge/start|answer|me`, `assert_user_match`, NO answer leak) wired in main.py. Flutter `app/lib/v3/challenge_screen.dart` (lobby→one-item climb→result with a mountain that fills to your altitude) + `services/challenge_service.dart` + entry card on the Olympiad home (`_climbEntry`). **Smoke `tests/smoke_challenge.py` 16/16** (high learner all-correct→533, low all-wrong→315 = genuinely adaptive; no leak; resume=no-jump-back; persist; IDOR 403). Added to `ship_gate.sh`.
- ⚠️ **CALIBRATION CAVEAT (real):** `irt_b` is heuristic, `a` fixed → present the rating **modestly** ("sharpens as you play"), NOT a validated percentile. Acing 10 *easy* L1 items → ~533 (correctly not "genius"). Path to a real score = log Climb responses → `scripts/irt_calibrator.py` over time (the response data is itself a moat). Doc: `ADAPTIVE_CHALLENGE_DESIGN.md`.
- **Ship:** backend `./ship_gate.sh` (GREEN) then `./deploy.sh` (streak field, contest-today already there, Climb endpoints) + **APK** `flutter build apk --release -t lib/main_v3.dart` for celebration + streak nudge + live banner + The Climb screen. All Flutter is structure-checked only (no SDK in sandbox) → **device QA still owed**.

## ⚠️ REORGANIZED 2026-06-12 — READ FIRST
- **Canonical content = `content-live/`** (content-live/content-v2 + content-live/content-v4). All question QA/fixes happen ONLY here. Old paths `content-v2/`, `content-v4/` moved under it; deploy.sh/main.py/pre_deploy_check.py updated.
- **`archive/`** holds everything not served: content-production (never wired, ~97% overlap with live), Benjamin olympiad (QUARANTINED — placeholder answer keys, do not serve), old docs/tools/strays. See archive/ARCHIVE_INDEX.md.
- Full question QA done 2026-06-12 (QA_FULL_REPORT_2026-06-12.md): 0 critical issues in content-live; 209 unanswerable deleted (recoverable in qa-reports/); hints de-leaked. Security hardening + API auth + Firestore persistence done (FIXES_APPLIED_2026-06-12.md).

## ⚠️ LEVEL RE-TAG 2026-06-13 — Olympiad section is now LEVEL-based (L1–L8), not grade-based
- New tagging system per uploaded `00_TAGGING_SYSTEM.md` + `01_TOPIC_MAP.md`: every olympiad question carries a **Level (L1–L8)** + internal **Pillar (NT/ALG/GEO/COM, never shown to users)** + a friendly **Topic display name**. Levels: L1=G1/2, L2=G3/4, L3=G5/6, L4=G7/8, L5=G9/10(IOQM), L6=RMO, L7=INMO, L8=IMO.
- **Two consolidated stores created** (full reorg): `content-live/olympiad/L{1-8}/{Ln}_{PILLAR}_{topic}.json` (**18,803 olympiad qs**, new IDs `KM-L{n}-{PILLAR}-{serial}`, old id kept as `legacy_id`; `topic_map.json`, `id_map.json`, `counts_report.json` alongside) and `content-live/curriculum/{board}/grade{n}/` (**10,340 school-curriculum qs**, school-only, **grade-tagged** `km_grade` 1-6 verified vs original_id+school_grade = 0 mismatches, chapters.json copied, refs still 78% resolve). Curriculum by grade: G1=1,663 G2=1,612 G3=1,581 G4=1,684 G5=2,065 G6=1,735.
- Pool split = original_id prefix: olympiad = T/GEN/PCT + all v2 topic-1..8 + wavebook; curriculum = NCERT/IGCSE/ICSE/SING/USCC. Conflicting curriculum tags (chapter, curriculum_map/source/tags, dual_tagged) STRIPPED from olympiad qs.
- Integrity VERIFIED: answers/hints/difficulty/IRT/SVG byte-identical to source (0 mismatches, hash + field-by-field). Counts: L1=9,660 · L2=6,320 · L3=2,823 · **L4–L8=0 (empty structure, fill later)**. Report: `KM_LEVEL_REORG_REPORT_2026-06-13.md`.
- **Old `content-v2/` + `content-v4/` banks are NOW SUPERSEDED but still on disk** (kept so backend doesn't break pre-cutover). NEXT PHASE: (1) UI/UX level-picker for Olympiad section, (2) rewire backend (content_store_*, deploy.sh, pre_deploy_check.py) to read olympiad/+curriculum/, then archive content-v2/content-v4.
- **OLYMPIAD QA + FIXES done 2026-06-13** (KM_OLYMPIAD_QA_REPORT_2026-06-13.md): 7-dimension scan. Bank **18,803→18,099** (704 hidden duplicates removed, exposed after stripping ~6,650 story-wrapper filler prefixes — "On the world tour, X runs the numbers." etc., logic puzzles protected). Hints: 1,393 answer-leaks masked + 545 missing generated → **0 leaks**. 550 over-tagged required-visuals downgraded to optional. 2,064 placeholder visual_context cleared. 1 truncated stem fixed (KM-L3-NT-0574). Integrity: choices/answer/IRT/difficulty 0 mismatches every pass. Final L1=9,298 L2=5,978 L3=2,823. Residual: ~103 bespoke filler intros, generic-hint quality.
- **CONTENT QA #2 done 2026-06-14** (KM_CONTENT_QA_2026-06-14.md): triggered by live-app bug KM-L1-NT-1164. Whole served bank (content-live/olympiad+curriculum — verified it's the LIVE /v3 copy, not superseded content-v2). **~6,937 stems** stripped of decorative filler across 5 safe passes (name-wrappers "A crystal on the cave wall glows near Chikoo.", no-name scene templates "With tools and parts.", statement-style logic "The machine needs Kiwi to solve a puzzle. Negate:…", colon/dash intros "Help Vanya calculate:" / "The demon lord's final challenge — Ved thinks it through!", old character names Knight Koko/Chef Cheetah/Builder Bee). Guards: never cross a sentence with a digit/operator/`:`/quote/math-object (protected flag-reflection, paper-folding, magic-square, age-comparison content); skip dangling-pronoun remainders. **2,461 placeholder visual_svg** removed (grey box+label fakes #F8F9FA/#6C757D — only 8 even referenced a figure & all answerable from text; 6,108 real diagrams kept); **1,136 exact duplicates** + **4 empty stems** removed (logged qa-reports/removed_dups_2026-06-14.json). Integrity: answers/choices/hints/IRT/difficulty **0 mismatches** vs backup (qa-reports/backup-2026-06-14/, 138 files), 0 phantom ids, smoke 17/17. **New totals: olympiad 18,099→16,963 (L1 8,766 · L2 5,374 · L3 2,823), curriculum 10,340→10,336, served 27,299.** counts_report.json regenerated; smoke + pre_deploy_check expected-counts updated to 16,963/10,336. Image mechanism = inline `visual_svg` per question (served inline + /v3/olympiad/question/{id}/visual; app renders SvgPicture.string). Needs `./deploy.sh` to ship (content baked into image).
- **UI PROTOTYPE built** (`kiwimath_prototype.html`, openable): modern 4-tab redesign (Olympiad levels / School grades / Progress / Profile), brilliant.org-style line icons, no question counts in Olympiad, numerically-sequenced chapters, Academic Height gauge (200-800 scale, 500=avg, 800=ceiling), 4-pillar + Logic&Puzzles strands, Profile with edit/switch/sign-out, and a UNIFIED ECONOMY (one shared state → wallet+Progress+Profile always match; practising earns XP/coins/gems/streak, awards unlock live; jsdom-verified no-disjoint). To become Flutter next.
- **BACKEND REWIRE done 2026-06-13** (serves the remapped banks live): new `backend/app/services/content_store_level.py` (lightweight LQ wrapper — QuestionV2's id-validator rejects KM-* ids; loads olympiad 18,099 + curriculum 10,340) + new `backend/app/api/level.py` (`/v3`): olympiad levels/topics/next/question/visual, curriculum boards/grades/sequenced-chapters/questions, **server-side `/v3/answer/check`** (grades + drives ONE economy via `gamification.record_answer`, idempotency-keyed) + `/v3/me/wallet` + `/v3/me/progress` (academic height + strands from the SAME state = no disjoint). Wired into `main.py` (router + startup bootstrap + /health). `deploy.sh` bakes content-olympiad/+content-curriculum/ and sets `KIWIMATH_OLYMPIAD_CONTENT_DIR`/`KIWIMATH_CURRICULUM_CONTENT_DIR`. `pre_deploy_check.py` verifies new banks (+ fixed a pre-existing inline-SVG false-positive). **Smoke test 17/17 PASS** (`backend/tests/smoke_level_v3.py`): no answer-leak at fetch, economy consistent, idempotent. **REMAINING last mile:** point the Flutter app at `/v3` (it still calls /v2,/v4); the prototype is the screen spec.

- **ANSWER-KEY VALIDATION started 2026-06-14** (triggered by live bug A1-ADD-0315 "Raju has 7 watermelons, gets 5 more" keyed to 2 not 12): prior QA treated correct_answer as a LOCKED field and never checked math correctness — that's how it slipped. Built an arithmetic validator (single-step integer add/sub/mul/div word problems + explicit "what is A op B"; excludes fractions/decimals/percent/geometry/combinatorics/multi-step "times as many"/"how many more"). Found + fixed **11 wrong keys**: A1-ADD-0315, KM-L1-NT-0039/0140/0429/0432, KM-L1-ALG-0314/0447, KM-L1-COM-0721/0771/0804 (all keyed to a distractor, usually the add-result on a mult question e.g. 3cats×4legs keyed "7"=3+4), + KM-L3-ALG-0326 (percent: "sold 35% of 200, how many left" keyed 70=sold, fixed to 130=left). Integrity: ONLY correct_answer/correct_value/diagnostics changed on exactly those 11; choices/stems/hints/IRT 0-change vs backup; smoke 17/17. **NOT YET VALIDATED (could still hold wrong keys): fraction/decimal arithmetic, multi-step word problems, combinatorics — each needs its own validator (next pass).**
- **GEOMETRY pass done 2026-06-14** (learn-from-images-then-apply): rendered geometry SVGs (cairosvg) and viewed them. Finding: geometry ANSWER KEYS are clean — formula validator (rect/square area+perimeter, triangle area, angle-sum; excludes composite "divided/cut out/attached/inscribed/sector") checked 940 simple-geometry qs → **0 mis-keyed** (composite-figure hits all verified correct). Real geometry defect = **mismatched template images**: the SVGs are reused generic templates (one clock, one triangle, one rect) and got mis-assigned. Built figure-type classifier (clock=12 texts+14 lines+clock-nums, triangle=3-pt polygon, rect=single rect, circle) vs stem shape; **removed 52 clearly-mismatched images** (clock on "square rotated 90° clockwise" KM-L3-GEO-0387, rect on cardioid/circular-paper/sector/sphere qs, etc. — all text-answerable so a wrong figure only confuses). Integrity: only visual_svg/alt/context cleared, 0 change to stem/choices/answer/hint; smoke 17/17. Note: "clockwise" first hid GEO-0387 (matched "clock") — fixed with negative lookahead. ~6,013 real diagrams remain.
- **FULL IMAGE-MATCH AUDIT done 2026-06-14** (all 6,013 image qs; render-and-view via cairosvg): images are reused generic TEMPLATES (1,673 unique SVGs, 439 structural). Figure-type classifier (clock=12-texts+clock-nums, coin=#FFD700 gold, data=Item-text/tally, grid=≥8 rects, triangle/rect/circle/angle=°+lines/numberline). KEY learning: most "circle" images are gold COINS on coin questions (match) + ray/angle diagrams misread as circles (match) — a blunt rule would delete good diagrams, so matched by DOMAIN not shape. **Removed 148 cross-domain-wrong** (133 clocks on rotation/direction/"clockwise" qs with no time kw, 14 coins on non-money, 1 tally-chart on "cut a cone") — grids on grid/coordinate qs correctly PRESERVED (75). **Recreated 14 required-but-missing** images (13 fraction "divided into N parts, M shaded" → generated accurate N-segment bar with M shaded #FF6F00; 1 SING-G5-SMC-023 square+inscribed-circle shaded). Integrity: only visual_svg/alt/context changed, 0 change to stem/choices/answer/hint vs backup (correct_answer diffs vs backup = exactly the 11 key fixes, 0 image-driven); smoke 17/17. Image bank now 5,879 (6,013 −148 +14). Images are BACKEND-served (baked in image) → ship via ./deploy.sh, NOT the APK. Residual ~5,879 are topically-matched decorative templates (low risk; could strip to essential-only per "visuals only where required" as a future pass).
- **OLYMPIAD UPPER-TIER IMPORT started 2026-06-16** (sources: IOQM at Desktop/MO/IOQM/, RMO at Downloads/Kiwimath_RMO_Deliverables/, INMO at Downloads/INMO/ — all from a parallel cowork space; user owns L4/grade7-8, I own L5-L8). Division: IOQM→L5, RMO→L6, INMO→L7. **L5 (IOQM) DONE**: 6 banks (Alg 200, Alg-NT-border 50, NT 200, COM 200, GEO 200, Comb-Geo 50) = **900 qs** → content-live/olympiad/L5/L5_{PILLAR}_{topic}.json, IDs KM-L5-{PILLAR}-{serial} (legacy_id=AL26-001 etc.), all INTEGER answers (gradeable), Geometry's 200 inline svg kept, hint+solution_steps+solution bundled, irt_b from difficulty. Verified: olympiad 16963→17863, L5 available, integer grading works, 0 answer-leak. Added empty-topic filter in level.py olympiad_topics (`if t.questions`) so empty scaffold topics (L4-L8 stubs) hide until populated. **L6 (RMO 800) + L7 (INMO 550) DONE 2026-06-16**: transformed → content-live/olympiad/L6,L7, IDs KM-L6/L7-{PILLAR}-{serial} (legacy_id kept), study-card model (450 numeric→integer-gradeable, 900 proof→reveal-solution), PNG figures base64-embedded into each question's `visual_png` (all 1350 decode), Combinatorial-Geometry split as own topic. App: added flutter_math_fork (MathText renders mixed prose+`$LaTeX$`), Image.memory for base64 PNG, reveal-solution/proof view in PracticePage (lib/v3/kiwi_v3.dart). Backend: LQ wrapper + `_q_public` now carry visual_png + solution_steps + solution (proof reveal; answer still omitted), empty-topic filter (level.py `if t.questions`). L5 math-format upgrade: 40 IOQM polynomial stems "1x^3+-..." → clean LaTeX `$x^3-3x^2+2x+5$`, 16 subscripts C_5→`$C_5$`, hint reworded, 0 answer changes. **olympiad 17863→19213** (L5 900·L6 800·L7 550 + L1-3). smoke 17/17, pre_deploy green, counts→19213. SHIP = backend ./deploy.sh (content+`_q_public`) **AND** APK rebuild (-t lib/main_v3.dart; needs flutter_math/proof/PNG render).
- **L4 (Grade7-8 800) DONE 2026-06-16** (source Desktop/MO/L4/Grade7-8_*_Questions.json — 4 banks Alg/Com/Geo/NT 200 each, same IOQM format: integer answers, inline svg, hint+solution): same pipeline → content-live/olympiad/L4/L4_{PILLAR}_{topic}.json, IDs KM-L4-{PILLAR}-{serial} (legacy A7-001 etc.), integer-gradeable, math-normalized (caret→LaTeX/subscripts/+-), irt_b easy-.5/med.5/hard1.5. The 11 loose .jpg in MO/L4 are unused scratch (0 referenced). **olympiad 19213→20013.** Smoke checks repointed L4→L8 (L8/IMO now the only empty level). **LADDER COMPLETE grade1→INMO: L1 8766·L2 5374·L3 2823·L4 800·L5 900·L6 800·L7 550; L8(IMO)=0 coming-soon.** smoke 17/17, pre_deploy green.
- **L4–L8 "3-TYPES" QA done 2026-06-17** (triggered by 3 live screenshots — KM-L4-GEO-0013/0015/0152): scanned all L4-L7 olympiad (L8 empty). **Type A unsubstituted template placeholders** (math-stripped + SVG-brace detector; excluded LaTeX `\dfrac{}{}`, `Σ_{k}`, `x^{2k}`): 20 qs fixed — `{b}`k→2nd ratio term in 4 angle SVGs (GEO-0012/13/14/15), `{j}`→#ones in 4 L5-COM hints (0181-84), `{n}`→#chords in 2 L5-GEO hints (0229/30), `{rem}`→remainder in 4 L7-ALG solutions (0021-24), `Erd\H{o}s`→Erdős in 6 L6 stems/solutions (COM-0072/0114, NT-0040/41/42, GEO-0250). **Type B wrong answer key**: validators (N-part angle-ratio + remainder-theorem P(a)) found **1** — KM-L4-GEO-0015 (ratio 7:2 "larger angle" keyed 40=2·20 instead of 140=7·20; generator always took 2nd term as larger, wrong when 1st>2nd). Fixed correct_value/answer + solution. Similar-triangle L5-GEO-0161-76 and 3-part triangle ratios re-validated CORRECT (false positives from naive "tri**angle**" substring match). **Type C mismatched image**: 2 semicircles drawn as FULL circles (GEO-0151 r7, 0152 r14) → redrew proper semicircle (render-verified flat-diameter+arc-on-top via cairosvg). Integrity: 978 qs byte-identical to backup-3types-2026-06-17/, exactly 22 changed, **0 unexpected field changes**, only GEO-0015 had answer-key change. 0 placeholders remain, smoke 17/17, pre_deploy green (olympiad 20013 unchanged). Changelog qa-reports/3types_changelog_2026-06-17.json. Content-served → ship via **./deploy.sh** (no APK rebuild needed; inline visual_svg).
- **Type D "confusing formation" done 2026-06-17** (triggered by live screenshot KM-L4-ALG-0198 "which figure number has exactly 21 dots?" — references a figure/dots model with NO figure shown): principled detector = stem promises a visual ("shown"/"the figure"/"the diagram"/"figure number"/"dots"/"shaded") but visual_svg+visual_png both empty → across all L4-L7 returned **exactly 3** (KM-L4-ALG-0198/0199/0200, the reverse odd-number-pattern qs). The forward siblings 0188-0194 ("how many dots in figure 5?") DO have a real labelled dot-figure SVG (Fig1-4 = 1/3/5/7 dots, render-verified) → left as-is, they're standard & clear. L5-GEO-0151-60 "L-shaped figure" use "figure" in normal geometric sense → not this error. Fix: reworded the 3 stems "which figure number has exactly N dots?" → "In the sequence of odd numbers 1, 3, 5, 7, …, what is the position of N? (1 is the 1st, 3 is the 2nd, …)" — answer preserved (11/16/21 = (N+1)/2), only `stem` changed (hint/answer/solution_steps/IRT untouched), integrity 3 changed/0 unexpected, smoke 17/17, olympiad 20013 unchanged. Ship via ./deploy.sh.
- **Type E "figure reveals the solution" + MISTAKES REPOSITORY done 2026-06-17** (triggered by 2 live screenshots — Venn KM-L4-COM-0148 showing regions 11|9|7=27, tree KM-L4-COM-0001 drawing all 12 leaves): scanned olympiad L1-L7 + curriculum. **Type E = figure pre-solves the question.** Removed **29 spoiler figures** in L4_COM: 16 Venn diagrams (COM-0147-0162) that showed the derived region decomposition (11,7 not in stem) + 13 tree diagrams (COM-0001/0003/0129/0133-0145) whose leaf-count = answer; also dropped "Using a tree diagram," from 5 stems. All text-answerable; answer/choices/hint untouched. The "answer-literally-shown" hits in L1-L3/curriculum were MCQ false positives (correct_answer is a choice INDEX 0-3, figures show givens). **Also extended Type C to L1-L3** (comprehensive scanner found them): redrew **13 semicircle/quarter-circle figures** drawn as FULL circles (10 in L1_GEO_shapes_2d_3d "what is the radius", 3 in L3_GEO_area_perimeter_volume area/perimeter; KM-L3-GEO-0220=quarter) — render-verified, labels preserved; excluded false positives (inscribed/central angle, paper-folding). **Also Type D in L2-GEO**: reworded KM-L2-GEO-1022 ("the figure given consists of two squares…" no figure, perimeter arrangement-dependent → specified arrangement, answer 44 preserved) + KM-L2-GEO-1014 (shaded-region, no figure → self-contained, 21 preserved). **Built a reusable read-only scanner `content-live/qa-reports/content_qa_scan.py`** (6 detectors: placeholders/ratio-key/semicircle/absent-figure/Venn+tree spoilers; MCQ-aware, set-notation-aware) — currently reports **0 flags** across all served content. **Built `qa-reports/MISTAKES_REPOSITORY.md`** — full handover catalogue of all ~18 content-defect types (description/why/detector/example IDs/fix/count) + golden rules + false-positive guards, for coworker handover. Changelog 3types_changelog_2026-06-17.json now 85 edits (A27·B4·C15·D5·E34). pre_deploy green (olympiad 20013 unchanged), smoke 17/17. Ship via ./deploy.sh.
- **CONCEPT CLUSTERING for adaptive layer done 2026-06-17** (user: group original-concept vs repetition, tag clusters, count per level — precursor to adaptive design; user chose SKILL grain over template grain): tagged ALL 30,349 served questions (olympiad 20,013 + curriculum 10,336) with a **skill_id** (one concept = number-varied + wording-varied copies collapsed). Method = stem→signature (numbers→#, char-names + "Help X figure out:" stripped, ops normalized ×→mul ÷→div −→sub so arithmetic facts don't merge, math structure kept) → **leader clustering** Jaccard≥0.70 (NOT single-linkage union-find — that chained number-seqs+shape-patterns into a 656 blob). **8,205 concepts total.** Per level (questions→concepts, repeat×): L1 8766→1916 (4.6), L2 5374→461 (11.7, most templated — e.g. 182 rectangle-area variants), L3 2823→2456 (1.1, bespoke olympiad, mostly singletons), L4 800→184 (4.3), L5 900→184 (4.9), L6 800→379 (2.1), L7 550→330 (1.7); olympiad 20013→5910, curriculum 10336→2295. Added fields (additive, **0 existing-field changes** verified by per-q corehash): `skill_id` (SK-{scope}-{NNNN}), `skill_size`, `skill_rank` (difficulty order within concept = ready ladder), `is_skill_original` (1 canonical exemplar/concept). Index `content-live/skill_clusters.json` (8,205 → label/scope/topic_file/size/difficulty_range/members). Reusable idempotent tagger `content-live/qa-reports/cluster_concepts.py` (re-run after imports; threshold tunable). Size-distribution = where adaptive pays off: L1/L2 have big families (116/87 concepts with 16+ variants → ladder within); L3/L7 mostly unique (broaden across). Report `qa-reports/CONCEPT_CLUSTERS_2026-06-17.md`. smoke 17/17, pre_deploy green. Hierarchy now Level→Pillar/Topic→Concept(skill_id)→item(skill_rank). NEXT: design the adaptive layer on these clusters. Ship via ./deploy.sh.
- **ADAPTIVE SKILL-LADDER ENGINE done 2026-06-17** (user spec: skill question = cluster parent, cluster questions = variants; tag skills in difficulty order; cluster Qs inherit parent difficulty; **right on skill → next skill (skip cluster), wrong → drip cluster Qs till one right or exhausted → next skill; remember position so re-login never jumps back**). Extended `cluster_concepts.py` tagger to also emit **`skill_seq`** (skill's rung on the topic ladder, ordered by skill-question difficulty) + **`skill_difficulty`** (parent's b, inherited by all cluster members) — re-ran, 30,349 q tagged, 0 existing-field changes. Added those + skill_size/rank/is_skill_original to `LQ.__slots__` in content_store_level.py. New service **`backend/app/services/adaptive_skill.py`** (`AdaptiveSkillEngine`): builds per-(level,topic) ladder from tags; `next_qid` (pure read), `record` (advances per rule, **monotonic/no-regress**), `status`; position persisted in **`FirestoreBackedStore("adaptive_skill_state")`** (durable across logins + in-mem fallback) keyed by user → `{ "L4|geometry":{pos,cursor} }`. Wired into `level.py`: `/next` now **default `mode=skill`** when user_id given (returns rung + `adaptive` block; `mode=irt`/no-user falls back to old ZPD), `/answer/check` calls `engine.record` + returns new `adaptive` status, new `/adaptive-status` endpoint for resume UI. Test `backend/tests/smoke_adaptive_skill.py` **18/18** (skill-right→next-skill, skill-wrong→cluster-drip, cluster-right→next-skill, exhaust→next-skill, **fresh-engine resumes same q = re-login no jump-back**, no-regress, full API round-trip). v3 smoke still **17/17**, pre_deploy green (20013 unchanged). Current app already sends user_id to next+answer/check → rule activates on **./deploy.sh, no APK rebuild needed** (optional app polish: show "Skill X of Y" from `adaptive` block, hide manual skip in skill mode). Doc `qa-reports/ADAPTIVE_RULESET_2026-06-17.md`. **Pre-deploy code QA done 2026-06-18** (independent subagent review): fixed IDOR on `/v3/.../next` (added assert_user_match — cross-user now 403), hardened `record()` with re-read monotonic guard (no-regress under concurrent writes), defensive int parse in `_get_pos`, error guard on `/adaptive-status`. Data integrity perfect (0 missing tags, 0 cluster disagreements). Verdict SAFE TO DEPLOY. ⚠️ Follow-up before next re-cluster: add ladder version stamp so saved positions don't reinterpret. Doc `qa-reports/ADAPTIVE_CODE_QA_2026-06-18.md`.
- **Type F "figure labels the answer" done 2026-06-18** (live screenshot KM-L4-GEO-0059 "right triangle legs 3,4, find hypotenuse" — figure drew the hypotenuse "5"=answer, and legs 3/4 in tiny font): detector = geometry `<polygon>` figure whose integer answer appears as a `>N<` label AND not in stem, excluding clock/number-line/graph (scale numerals collide). Found **17** (right-triangle hypotenuse family: KM-L4-GEO-0059-0063 + KM-L5-GEO-0033-0044). Correctly LEFT alone "find area/other-leg" variants (answer not labelled) + number-line "Compute" FPs (polygon=arrowhead). Fix: parametric redraw showing only the two legs, font-size 20 (was 13), right-angle mark, NO hypotenuse label — render-verified cairosvg. Only visual_svg changed, 0 locked-field/skill-tag changes, scanner now 7 detectors all 0, pre_deploy green (20013), smoke 17/17 + adaptive 18/18. Added `scan_answer_in_figure` (section F) to content_qa_scan.py + entry #19 to MISTAKES_REPOSITORY.md. Changelog 102 edits (A27·B4·C15·D5·E34·F17). Ship via ./deploy.sh.
- **Type G "angle label on wrong vertex" done 2026-06-18** (live screenshot KM-L4-GEO-0049 "right triangle one acute angle 30°, find the other" — "30°" was drawn ON the right-angle corner that carries the square 90° mark, contradicting the figure; labels tiny too): detector = right-angle mark is a 3-pt `<polyline>` whose middle point = 90° vertex; flag a degree `<text>` (has digit + °) within 28px of it. Found **4** (KM-L4-GEO-0049-0052, given 30/55/18/62°). Fix: redrew with right-angle square at 90° vertex, given angle at the apex acute vertex, `?` at the other acute vertex, font 19 (was 16). Only visual_svg changed, 0 locked/skill-tag changes, scanner now **8 detectors all 0**, pre_deploy green (20013), smoke 17/17 + adaptive 18/18. Added `scan_angle_on_right_angle` (section G) + repository entry #20. Changelog 106 (A27·B4·C15·D5·E34·F17·G4). Ship via ./deploy.sh.

- **Type H "pattern visual missing" done 2026-06-21** (live screenshot KM-L1-ALG-2110 "What colour bead comes next?" — 4 colour options, NO image → unanswerable): root cause = a content pass shortened the served `stem` to a bare prompt expecting a visual, but `visual_svg` was None/empty (placeholder stripped) → pattern data gone. `original_stem` still held it ("…beads: Red, Blue, Red, Blue, Red, Blue, ___…"). Fix = rebuild a valid pattern visual whose cycle yields the keyed answer: sequence from `original_stem` when present (+ verify next==answer), else inferred 2-item `[answer, other]` where `other` = the diagnostics' "which colour follows X" or a distractor (seq ends on other → next == answer always). Generators in `qa-reports/fix_pattern_visuals.py` (`--kind color|shape|size`, idempotent, backs up): coloured **beads on a string**, outlined **shapes** (circle/square/triangle/star/heart/diamond/…), size-graded **circles**, each ending in a dashed "?". **69 questions fixed, 100% answer-consistent** (colour 22, shape 37, size 9, +1 number stem-restore KM-L3-ALG-0587). Integrity: only `visual_svg`/`visual_alt`/`stem` changed vs backup-pattern-visuals-2026-06-21/; all SVGs valid; files re-indented to repo style; render-verified (beads/shapes/sizes look clean). **9 FLAGGED not auto-fixed** (data unrecoverable — no original_stem + mixed-attribute/number choices; would be fabrication): KM-L1-COM-0711, KM-L3-ALG-0499, KM-L1-GEO-0136(unicode), A2-PAT-0337(emoji), A1-PAT-0576/0580/0583/0590/0594. Added scanner detector **H** to `content_qa_scan.py` + report `qa-reports/PATTERN_VISUALS_FIX_2026-06-21.md`. Content-served → ship via ./deploy.sh.

- **Type I "mismatched/decorative figure on a logic question" + LEVEL-QA LOOP method (2026-06-21)** (live screenshot KM-L1-COM-3116 "Which one does not belong: 28,21,26,20?" showing an irrelevant **mirror** figure, visual_alt="topic-8-puzzles visual"): root cause = a reused generic-template SVG auto-attached to logic/number questions that need no figure (28,21,26,20 → 21 is the only odd; answerable from text). NOT from the Type-H pattern fix (that only writes bead/shape/size SVGs). Found via grouping L1 svgs → **28 templates reused ≥8× across 4-6 unrelated topics**. Fixer `qa-reports/clear_mismatched_visuals.py` (--apply, backs up): in a logic/sorting/odd-one-out/missing_number topic, no figure-reference in stem, no undefined variables (A+B), ≥2 digits present, plain choices → **clear visual_svg/visual_alt + visual_requirement="none"**. **316 cleared in L1** (only visual fields changed vs backup-mismatch-visuals-2026-06-21/, answers intact). Added scanner detector **I** to content_qa_scan.py. **The repeatable per-level QA LOOP** (answer to founder's "how do I check a whole level for every mistake type"): you do NOT eyeball 8,766 qs — run the **detector harness `content_qa_scan.py`** (one check per mistake type A–I), fix every flagged q, re-run, repeat until TOTAL=0; when a new defect appears (like this), add a detector for it first. **L1 state now: A–G=0, H=9 (pattern-missing, unrecoverable → need source), I=24 (figure on variable/non-self-contained stems → manual review). 262→33 flags.** Ship via ./deploy.sh.
- **MASTER MISTAKE LIST + full-lens L1 re-scan (2026-06-21)**: founder asked to "list every mistake type seen so far, re-check L1 against all of them, then L2." Updated `qa-reports/MISTAKES_REPOSITORY.md` to **22 types** (added #21 Type H pattern-missing, #22 Type I mismatched-figure). Extended `content_qa_scan.py` with 4 more detectors → **A–M**: J empty/truncated stem, K answer-leak-in-early-hint (PRECISE: requires "is/=/answer X" context + skips short/common words — a naive version gave 308 false positives from "No"/"triangle" in generic hints), L true-duplicate (stem+choices+answer+**visual-hash**, so pattern qs whose figure carries content aren't falsely matched — naive version over-flagged 217), M fake-placeholder-svg. **L1 full-lens result: A–G=0, J=0, K=0, M=0 (clean); H=9, I=24 (flagged, known); L=99 TRUE duplicates (NEW finding — identical incl. visual; some created by Type-H inference giving same pattern to same-answer size/colour qs).** The 99 dups are real but removing them cascades into skill_clusters.json + counts_report + pre_deploy/smoke expected counts + adaptive ladder → do as a dedicated dedup+re-cluster pass (cluster_concepts.py idempotent), not a rushed delete. NEXT: dedup L1 then repeat the A–M lens on L2.
- **L1 LOOP COMPLETE — scanner A–N = 0 (2026-06-21)**: finished the L1 pass. **First caught a real detector bug**: detector L keyed on the raw `correct_answer` **index**, but that's a 0-based MCQ index — two "Which direction now?" items with the same index but **shuffled choices** have *different* real answers, so the index-key falsely merged distinct questions (and missed same-value reorders). **Fixed `scan_duplicates` to key on the resolved VALUE `choices[correct_answer]`** (true L1 dups 99→corrected). Then the value-key + manual review surfaced a much bigger L1 defect than dedup: **`L1_GEO_spatial` had 268 "Which direction is X facing now?" rotation questions that are unanswerable** — empty visual, no `original_stem`, even the hint is off-topic ("Let's think about shapes!"); the setup (start dir + turn) was lost when a multi-step series got flattened. Added **detector N** (`scan_directional_unanswerable`, guards self-contained "Meera faces West and turns right…" + "He goes up 3 steps…"). **Removals (L1 8766→8478, −288):** 134 value-based dups + **139 directional-unanswerable** (273 union) + 4 unrecoverable (COM-0711/GEO-0136 pattern-no-sequence; ALG-0533 change-no-setup+₹/$ mismatch; ALG-0774 "what time" but the clock figure shows 3:00 ≠ key 8) + 11 dups newly-exposed after stem restore. **Restored 22** Type-I-tail logic stems from `original_stem` via anchor extraction + verify-the-key-still-follows (A+B+A `If A=x and B=y…` arithmetic-checked, coin-comparison most/fewest-checked incl. dropped-name fixes like COM-1018 "Kiwi…"→"Priya…" matching its choices, which-doesn't-belong), clearing their decorative `mirror`/₹/shape figures. Integrity: kept questions byte-identical to backups (0 locked-field changes), every removed dup has a surviving twin. Re-ran `cluster_concepts.py` (30,076 q → 8,204 concepts, integrity_bad=0). Updated counts_report.json + smoke (olympiad 20013→**19725**) + pre_deploy. **smoke 17/17, pre_deploy green, scanner L1 A–N all 0.** New scripts: `qa-reports/remove_l1_dups_unanswerable.py`, `fix_l1_decorative_logic.py`; backups `backup-dedup-l1-`/`backup-l1-decorative-2026-06-21/`; manifests `removed_l1_2026-06-21*.json`. MISTAKES_REPOSITORY → 23 types (added Type N + the detector-L value-key fix). **Global (other levels) still flagged: J≈93, K≈32, L≈198, H≈7 — NEXT: run the A–N loop on L2.** Ship via ./deploy.sh.
- **L2 LOOP COMPLETE — scanner A–N = 0 (2026-06-21)**: ran the same A–N lens on L2. Only J + L fired; everything else 0. **Hardened two detectors first** (their crude versions were noisy): (1) **Detector J** was flagging valid sentence-completion MCQs ("…is coded as ___"), conversions ("Rs 5 and 75 paise = ? paise"), figure-backed prompts ("…the boxes given below" + svg), inline-pattern items ("A, B, C, A, B, C, A, ?"→B, letters/colours so no digit), and concrete prompts ("interior angle of a regular hexagon"=120°, "Find all n with φ(n)|n") — 93→precise. New J = empty / one bare word / **absent-referent** (missing number · what comes next · sequence of figures · arrange the following) with no digit, no inline comma-list, no figure, and NOT "which/consider the following" or "complete the pattern: <inline>". (2) **Detector L** now distinguishes **removable** dups (same level, or cross-level with SAME difficulty=misfile) from **intentional** cross-level pairs (same question calibrated at a different tier difficulty, separate files → never seen together) which it no longer flags. **L2 5374→5264 (−110):** 108 within-L2 value-based dups (kept one copy per concept) + 2 genuinely unanswerable (KM-L2-ALG-0232 "Find the missing number" no data/visual/original; KM-L2-NT-0430 "Arrange the following…" no list). The 36 cross-level L2=L1 pairs verified **different difficulty → intentional, KEPT**. J false-positive fix also re-confirmed **L1 stays J=0** (its 4 "what replaces the ?" items have the pattern inline → answerable). Integrity: 4598 kept L2 q byte-identical (0 non-skill field changes), every removed dup keeps a surviving twin, re-cluster integrity_bad=0 (29,951 q → 8,198 concepts). counts_report + smoke (olympiad 19725→**19615**) + pre_deploy updated; **smoke 17/17, pre_deploy green, scanner L1=0 AND L2=0**. New reusable `qa-reports/remove_level_defects.py --level Ln --ids …` (within-level dedup + explicit removes, backup+manifest); backup-L2-defects-/manifest removed_L2_2026-06-21.json. **NEXT: L3 (scanner already shows L3 J=9 absent-referent pattern qs — check recoverable-from-original vs remove — plus its dups; then L4–L7).** Ship via ./deploy.sh.

## Vedantu content library → taxonomy (2026-06-22)
- Founder uploaded `Grade wise content for Anand Sir.xlsx` = index of **6 Google Sheets** (Drive connector reads them) = the acad team's **competition-sourced curriculum Grade 3 → ISI/CMI/Putnam**: L3/L4 sheets = 40-session courses (DPP online test + assignment PDF + 10-test schedule); L5/L6/L7 sheets = topic assignment banks by **NT/ALG/GEO/COM/Trig** (our pillars); ISI/CMI sheet = 26 chapters (VDPP+Tatva+ISI-PYQ) + ~250 videos. ~205 assignment PDFs + ~60 reference textbooks. **Assignment PDFs extract cleanly via Drive `read_file_content`** = problems + answer key + **full worked solutions** + provenance (iOM/NSTSE). **KEY MAPPING (by GRADE, their "Level N" is offset +1):** their L3→our L2, L4→L3, L5→L4, L6→L5/L6, L7→L6/L7, ISI→L8+. Fills the thin tiers (L4 800·L5 900·L6 815·L7 550). **IP CAVEAT:** 3rd-party reference books (Evan Chen, Burton, Cengage, Andreescu…) NOT redistributable in store; team's own assignments/notes/videos are fine. Sheet IDs + full plan in **`VEDANTU_CONTENT_INTEGRATION_PLAN.md`** (repo root).
- **PHASE 0 PROOF DONE 2026-06-22** (1 topic end-to-end to de-risk the format): ingested L6→"GCD & LCM" (source sheet `1PxnB7p…`, PDF `1dqQq5S…`) → `content-live/olympiad/L6/L6_NT_gcd_lcm.json`, **15 questions** (IDs KM-L6-NT-0201–0215, legacy OMM-L6-NT-GCDLCM-NN), matching the existing L6 study-card model: **12 `integer` (gradeable) + 3 `proof` (reveal-solution)**, clean LaTeX, the PDF's worked `solution_steps`+`solution`, Socratic 2-level hints, irt_b/difficulty. **Added 2 NEW fields** carried by the bank + API: `source` (provenance) + `video_url` (per-problem YouTube solution) — wired into `content_store_level.py` LQ `__slots__`+`__init__` and `api/level.py _q_public` (served at fetch alongside solution; answer still omitted = no leak). **Answer-key validation:** brute-forced all 12 gradeable answers in Python vs the source key → **12/12 match** (source keys are correct, incl. #9 ordered-pairs=30). Scanner A–N on the new file = **0 flags**; **0 new duplicates** (the 48 L6 L-flags are pre-existing L6_GEO/ALG dups for the future L6 loop). Re-clustered (29,966 q → 8,212 concepts, integrity_bad=0; +14 new concepts). counts_report L6 800→**815**, olympiad 19615→**19630**; smoke **17/17**, pre_deploy green. Verified in-lifespan: topic "GCD & LCM" surfaces in L6 NT topics, fetch carries source+video, answer hidden. Reusable note: ingestion = Drive read_file_content → transform to L{n} schema (integer vs proof) → brute-force-validate keys → scanner → cluster → counts → smoke/pre_deploy → `./deploy.sh`. **Ships via backend `./deploy.sh`** (content baked + the 2 new fields are code); optional app polish later = render a "Source" line + "Watch video solution" button from the now-served `source`/`video_url`. NEXT (founder go-ahead): Phase 1 = bulk-ingest L5/L6/L7 topic assignments (~140 PDFs).
- **BATCH 1 STARTED 2026-06-22** (founder: "start batches, extra careful with images"; chose **sync Drive folder locally** for figures + start at **L6 NT+Algebra**). **CRITICAL FINDING — Drive `read_file_content` mangles math notation, not just images:** superscripts flatten ("Find the last digit of `20232023`" is really `2023^2023`); an OCR'd `+`→`.` turned `(1+3+5+…+97)` into a product (would flip the answer 1→5). **Safeguard that makes text ingestion safe = reconstruct each problem, then BRUTE-FORCE the answer in Python and require it to equal the source key, else HOLD.** Applied to 2 NT topics: **Cyclicity** `content-live/olympiad/L6/L6_NT_cyclicity.json` (12 integer, KM-L6-NT-0216–0227, all 12 brute-validated; **held 3** power-tower stems Q13–15 too garbled). GCD & LCM (Phase 0) already in. L6 815→**827**, olympiad 19630→**19642**, smoke 17/17, pre_deploy green, scanner 0 on new files, recluster integrity 0. Reusable `/tmp/ingest_lib.py build_topic(...)`. **Decision for the rest of Batch 1:** Algebra (fractions/quadratic graphs/GIF) + Geometry (figures) + all held items are **notation/figure-heavy → do from the SYNCED PDFs** (render page → true notation + extract figures), NOT text. Computational NT topics (last-digit/remainder/divisibility/counting) are safe text-only WITH brute-validate. **Held-for-rendered-PDF so far:** L6 Cyclicity Q13/Q14/Q15. Ship via ./deploy.sh.
- **CONTENT-HOME DECISION 2026-06-22** (founder: "why not an interactive book + use questions in daily quiz" — resolves the "don't dilute premium human content in the machine pool" worry). **Architecture = premium Vedantu content lives in its own two surfaces, NOT blended anonymously:** (1) **Library = per-pillar-per-level interactive books** (founder chose this slicing; e.g. "RMO Number Theory", "RMO Algebra"), built **faithful-page-render** (WebP of PDF pages) like the IOQM books — which *preserves exact math notation AND figures*, so the book SIDESTEPS both the exponent-mangling and the image-extraction problems (no reconstruction, no held items, geometry+proofs included natively). Books need the **synced PDF folder** (founder is syncing). (2) **Daily Quiz = a "Verified" pool** of the gradeable, brute-validated subset. Machine content stays as adaptive-practice volume. **VERIFIED-POOL WIRED + LIVE** (smoke v3 17/17, contest 23/23): added `verified` field to LQ (`content_store_level.py` __slots__+init, auto-true when a `source` is present) exposed in `api/level.py _q_public` + `contest_service._public` (badge+source); `contest_service.todays_qids` now **prefers the verified pool** when a level has ≥ N_QUESTIONS verified (else falls back to full pool — verified-probe: L6 daily quiz now serves 8/8 from GCD&LCM+Cyclicity all `verified:true`; L7 with no verified content still serves a full 8). So as verified content grows per level, that level's daily quiz auto-upgrades to premium. **NEXT:** once founder syncs the Drive assignment folder → build per-pillar-per-level faithful-render books (reuse `outputs/ioqm_build/` pipeline) + keep growing the verified quiz pool from the gradeable subset. Ship: backend `./deploy.sh` (verified field + contest pref are code; content baked) + APK later for the Verified badge UI + new books.

- **ORIGINAL AUTHORED L3 TEACHING BOOK — "Kiwi's Math Expedition" (2026-06-22)**: founder (as an Olympiad-cleared maths expert) asked for an L3 (Grade 5-6) book — fun/surprise-filled, Socratic/tutor-style, that **bridges the sharp L2→L3 jump**, covers the VOS L4-source (G5-6) topics + Kangaroo + **Vedic-maths speed tricks**. Reused the L2 engine: copied l2_helpers/l2_figs → `outputs/l3book/l3_helpers.py`/`l3_figs.py`, **extended with 3 figures** (angle(deg), ratio_bar([parts]), coord_grid([(x,y,label)])); `build_l3book.py` is the L2 book-style template (title page → tappable index → open flowing chapters + prev/next, A−/A+ + night) with a distinct sunset cover + the L3 taxonomy friendly names (Prime Hunters, In Proportion, Angle Chasers, Smart Counting, Sock Drawer Logic, Map Makers…). **23 chapters / 8 parts, 207 SVG figures, ~680 questions, 0.73MB.** New-for-L3 ideas scaffolded gently (integers, %, profit/loss, ratio, direct/inverse variation, exponents, real angle geometry, coordinates). **Part 3 · 🔮 Vedic Maths Magic** (Ch10) = the founder's surprise ask: ×11, squaring-in-5, same-tens/units-sum-10, Nikhilam base-100, all-from-9 subtraction, ×5/25/9/99, the 9-check — each taught with the *why* (place value/algebra, sympy-proven) so it's real maths. **Part 8 · 🦘 Kangaroo Corner** pitched to **Benjamin** level (parity/invariants/pigeonhole/painted-cubes/path-counting + a graded 3·4·5-point set). I wrote Ch1 (gold standard) + the figure extensions; chapters 2-23 by **7 parallel subagents** (each brute-force-verified every answer in Python — ~150 checks/agent, 0 wrong — + render-tested figures; fixed named→numeric SVG entities). Central QA: 23/23 build, tags balanced (svg 206/206, figure 207/207, details 680/680), 0 leaked objects, cross-section figure render clean. Registered in `store_service` (`l3-mathbook`, "Kiwi's Math Expedition", subject "Math Book", levels [3], free); smoke_store 25→**26 books**, claims+serves 200. Ship: backend `./deploy.sh`. NEXT (optional): same authored-book for L1; painted catalog covers.
- **L2 + L3 BOOK CRITICAL-AUDIT PRECISION PASS done 2026-06-23** (founder shared two rounds of expert review → "combine both and see what's suitable"): applied the genuine-error + branding + pedagogy fixes to BOTH authored books via 2 parallel editors (each rebuilt + brute-forced every changed claim in Python). **Genuine math fixes (L2 Ch17 cryptarithms):** AB+BA corrected from the false "always a 2-digit repeated number" → **AB+BA = 11×(A+B)**, 2-digit-repeated only when A+B<10 else 3-digit multiple of 11 (58+85=143); SO+SO=OO replaced hand-wave with clean **2(10S+O)=11O ⟹ 20S=9O ⟹ impossible**; AB+B=BA replaced guessing with **9A=8B ⟹ A=8,B=9** (L3 same cryptarithm proofs). **Other corrections:** L2 Ch19 Birthday Mystery now states "one clue may be wrong" upfront (was a silent Wed/Tue contradiction); L2 Ch7 decimals "pad to FOUR decimal places" (0.0123); L2 Ch14 letter-O "block-letter font, 2 lines; circle has infinitely many" + square "two shown, four in all"; L2 Ch16 typo "a day has a **night**" + likelihood ¼/¾ as *example* points + coin "fair-coin model"; L2 Ch13 + L3 Ch16 curved-surface-counts-as-a-face convention note; fold-and-punch caveat; L2 Ch4 "divisible by 5 AND even → ends in 0"; L2 Ch5 "4/4 = one whole (also called improper)"; L2 Ch8 tree→flagpole; L2 Ch1 missing figure caption added. **Branding:** all "real Math Kangaroo" → "Kangaroo-STYLE / in the style of" + "our own practice problems, not the official paper" disclaimers (L2 Ch21/22, L3 Ch22/23). **Pedagogy infra (both builders):** new `trap()` helper + `.trap` CSS = ⚠️ **mistake-trap boxes** (3 per book: fraction/decimal/%/profit common errors w/ why+fix); **Core/Stretch/Olympiad track badges** per chapter (TRACK dict + `_badge()` on index + chapter head — 44 L2 / 46 L3 chips). **Tone pass** (de-gushed "champion/medal/you-think-like-a-mathematician" → earned, method-specific praise). **Indian place value** strengthened: L2 Ch1 + L3 Ch1 now teach lakh AND crore side-by-side w/ international (12,34,567 vs 1,234,567). Rebuilt: L2 22/22 (552,987 B), L3 23/23 (733,941 B); catalog byteSize updated. **smoke_store 21/21, pre_deploy PASS.** Ship: backend `./deploy.sh` (content baked; no APK).

## Vedantu books — local pipeline + 7-pillar taxonomy (2026-06-22)
- **All 140 assignment PDFs downloaded locally** to `~/Downloads/Vedantu_Content/L{5,6,7}/{Pillar}/*.pdf` (69MB; founder used the generated `download_via_rclone.py` after gdown throttled). Sandbox reads them directly at `/sessions/…/mnt/Downloads/Vedantu_Content` — so figures + exact math notation are fully available (no connector, no mangling).
- **TAGGING RECONCILED (founder decision = "keep every strand separate"):** extend our 4 pillars to **7** — NT, Algebra, Geometry, Combinatorics + **Trigonometry, Basic Mathematics, Arithmetic** (each its own strand/book, mirroring the source). **Levels 1:1** (their L5→our L5 IOQM, L6→L6 RMO, L7→L7 INMO — content-difficulty aligned, matches Phase-0 GCD&LCM placement). Folder pillars per level: L5{NT,Alg,Arithmetic,Combi,Geo} L6{NT,Alg,Geo,Combi,BasicMaths,Trig} L7{NT,Alg,Geo,Combi,BasicMaths} → ~**16 per-pillar-per-level books**.
- **FAITHFUL-RENDER BOOK PIPELINE built + proven** (`outputs/vedantu_build/build_book.py`, reusable: `build_book.py <LEVEL> <PdfFolder> "<Display>" <Tier> <out_id>`): PyMuPDF renders each topic PDF's pages → WebP 2.0x/q72 base64; splits problem-pages vs the answers/solutions section (by page text "Answers Key"/"Solutions"); extracts youtu.be video links; assembles a self-contained interactive HTML — tiered cover, contents nav (smooth-scroll), per-topic problem pages + a `<details>` "Answers, video & solutions" reveal (solution pages + video buttons), night-mode. **First book built: `content-books/rmo-numbertheory/rmo-numbertheory.html`** (5.06MB, 9 topics, 57 pages, 135 video buttons) — figures + exponents intact (real PDF pages). NEXT: batch the other ~15 books, register them in `store_service` catalog (subject=pillar, levels=[tier], free), grow the Verified daily-quiz pool from the gradeable subset. Ship: `./deploy.sh` bakes content-books + catalog rows; APK for the Library UI.
- **ALL 16 BOOKS BUILT + REGISTERED + GREEN 2026-06-22.** Format per founder = **separate "▶ Video solutions" tab + "Answers & worked solutions" tab** (two `<details>`, not combined). `build_all.py` (resumable, time-budgeted, `fmt:2tab` marker so only old-format books rebuild; direct in-place overwrite since the sandbox mount forbids `rm`). 16 books in `content-books/{ioqm,rmo,inmo}-{pillar}/`, **109 MB total**: ioqm-{numbertheory,algebra,arithmetic,combinatorics,geometry} · rmo-{numbertheory,algebra,basicmaths,combinatorics,geometry,trigonometry} · inmo-{numbertheory,algebra,basicmaths,combinatorics,geometry}. Registered in `store_service.py` `_CATALOG` (16 rows: title "IOQM/RMO/INMO · <Pillar>", subject=pillar, levels=[5/6/7], `_FREE`) + `_BOOK_FILES` (id→html). `smoke_store` 22 books **21/21**; v3 17/17, contest 23/23, pre_deploy green. **Ship: backend `./deploy.sh` (bakes content-books + catalog) + APK** (Library lists catalog dynamically + routes format==html→HtmlBookReader, so they appear after deploy; APK only needed for the earlier reader/cover/parent-bug fixes). **Verified-quiz pool from these books' gradeable subset = the remaining follow-up** (bank-side 7-pillar question ingestion, separate from the books which are done).
- **DESIGNED COVERS + VOS BRANDING 2026-06-22** (founder: covers all looked same → "design a cover like a real book"; + "make it VOS Library (Vedantu Olympiad School) as we might rename the app"): rewrote `build_book.py` cover — **per-pillar deep palette + gold line-art motif** (NT=prime lattice, Algebra=parabola, Geometry=circumcircle+triangle+incircle, Combi=K5 graph, Trig=unit-circle+sine, BasicMaths=(a+b)² square, Arithmetic=ratio bars), tall framed front cover, tier badge, serif title, brand line **"VOS LIBRARY"**. Marker bumped to `fmt:2tab-cov`; rebuilt all 16 (still 109MB). Then direct in-place text swap KIWIMATH→VOS on the 16 (+builder) so branding = VOS without re-render. **App itself still named Kiwimath** — only the book-cover Library brand is VOS for now (rename is a maybe). **IOQM overlap decision = KEEP BOTH as-is** (old "IOQM 2026 · <pillar>" books + new Vedantu "IOQM · <pillar>" books coexist; revisit later). Ship: `./deploy.sh` (content-books baked).
- **GRADE-ALIGNED LEVEL REMAP 2026-06-22** (founder corrected the mapping: their sheet "Level N" is offset — sheet L3-L5 = our L2-L4 by GRADE, NOT 1:1). **Final mapping = sheet L5(G7-8)→our L4 · L6(G9-10)→our L5 IOQM · L7(IOQM/RMO)→our L6 RMO**; our L7/INMO filled later from ISI/CMI. (My earlier 1:1 IOQM/RMO/INMO labelling was one tier too high.) **Re-tiered the 16 books** in place (no re-render — just cover label + new id, old files truncated since sandbox can't rm): `ioqm-*`→`l4-*` (tier "GRADE 7–8"), `rmo-*`→`l5-*` (tier "IOQM"), `inmo-*`→`l6-*` (tier "RMO"); regenerated `store_service` catalog (16 rows, levels [4]/[5]/[6], titles "Grade 7–8/IOQM/RMO · <Pillar>"). **Moved the 2 ingested NT topics L6→L5**: `L6_NT_gcd_lcm`+`L6_NT_cyclicity` → `L5_NT_*` (KM-L6-NT-0201..0227 → KM-L5-NT-*, level/level_name L5/IOQM; old L6 files emptied). L5 900→**927**, L6 827→**800**, olympiad total unchanged 19642. Re-clustered (integrity 0). **Verified: L5 daily quiz now 8/8 verified (was L6); smoke 17/17 · store 21/21 · contest 23/23 · pre_deploy green.** **STILL PENDING (founder's "pick this up"):** (1) grow the Verified daily-quiz pool by ingesting the gradeable subset from the 140 local PDFs at the grade-aligned levels (L4/L5/L6) — same reconstruct+brute-force-validate pipeline as GCD&LCM. (2) Sheet L3 (G3-4)+L4 (G5-6) session-course sheets are NOT downloaded → map to our L2/L3, separate session-format ingestion. Ship: ./deploy.sh + APK (Library tier labels).

## Verified daily-quiz pool — bulk-ingested from the 140 PDFs (2026-06-22)
- **RANGE GRADING for fraction/decimal answers (founder: "in numerical answer key we can give range in case of fraction/decimal values")**: added optional `answer_min`/`answer_max` to LQ (`content_store_level.py` __slots__+__init__) + a shared `numeric_correct(q, sv)` helper — if a range is set, accept `lo ≤ typed ≤ hi`; else exact float (epsilon) / string match. Wired into BOTH grading paths: `contest_service._correct` and `api/level.py` answer-check. Unit-tested (integer exact · decimal/fraction range · float epsilon all pass). Extraction sets a ±1% range for non-integer keys.
- **BULK VERIFIED-QUIZ INGESTION DONE**: reusable `outputs/vedantu_build/ingest_quiz.py` ran across **all 140 local assignment PDFs** (`~/Downloads/Vedantu_Content/L{5,6,7}` = grade-aligned source→ours L5→L4, L6→L5, L7→L6) → **+1,172 gradeable questions** (olympiad **19,656→20,828**; L4 814→**1275**, L5 927→**1278**, L6 800→**1160**). Each = a **FAITHFUL cropped problem image** (`visual_png`, PyMuPDF 2.2x crop between consecutive "N." markers — preserves exponents/√/figures, sidesteps text-mangling) + answer from the source key + `source`+`verified` (→ Verified daily-quiz pool). **L4·L5·L6 daily quizzes now all serve 8/8 verified.** Mode split: **738 letter-MCQ + 449 typed-integer + 23 fraction/decimal-range** (+3 proof from gcd_lcm). IDs KM-L{4,5,6}-{PILLAR}-{serial}, 7 pillar codes (NT/ALG/GEO/COM/TRIG/BMATH/ARITH). The 2 hand-validated NT topics (gcd_lcm, cyclicity) are PROTECTED (never overwritten); proof-only topics correctly skip ("0 resolved" → they live in the Library books).
- **CRITICAL ANSWER-RESOLUTION LESSON (caught in QA)**: NEVER convert MCQ option text to a number — the PDF text stream mangles fractions/superscripts (e.g. option "1046½" extracts as `1 · 1046 · 2`, so a numeric grab returns the fraction's "1", silently WRONG — found via KM-L4-ALG-0206). Fix = keep MCQ problems as **real letter-choice MCQs graded by the key's LETTER INDEX** (choices ["A","B","C","D"], correct_answer=idx); the faithful image shows the real options, student taps the letter, no numeric parsing. Verified on the AP topic: resolved indices d,c,a,d,b,d exactly match the source key. Only direct-numeric-answer problems use typed grading. Guard: a letter key is only accepted if the crop shows a clean contiguous (a,b,c…) option set, else SKIP.
- **Scanner detector L made png-aware**: dup signature now hashes `visual_svg` OR `visual_png` (Vedantu items share a generic stem + empty choices, so the image is the only differentiator) — cleared 47 false-positive png-dup flags bank-wide (127→80; the 80 are pre-existing real L3–L7 flags for the future loops). **New Vedantu files add 0 scanner flags.**
- Re-clustered (31,164 q → 8,420 concepts, integrity_bad=0). counts_report + smoke (olympiad→20,828) + pre_deploy updated. Hardened the adaptive smoke fixtures (topic/skill selection was brittle to new content — now searches for a rich ladder with clustered skills 0+1). **GREEN: v3 17/17 · contest 23/23 · store 21/21 · adaptive 18/18 · pre_deploy PASS · scanner new-files 0.**
- **SCOPE (founder: "everywhere, badged" then "no badge required, do it at backend, for future reference"):** these questions are full citizens of the olympiad bank — they surface in **daily quiz (verified pool) + olympiad practice (37 new L4 topics, etc., per pillar) + adaptive skill ladder (all clustered) + daily contest/league**. `verified` is a **BACKEND-ONLY flag** (no UI badge) whose only job is to make the daily-quiz pool prefer this content. **Per-problem video-solution links patched on** (`video_url`, 1,155 qs from the PDFs' "Video Solution Links" block) — served in the **practice** payload (`api/level._q_public`) as a reveal, deliberately **NOT** in the rated **contest** payload (no mid-quiz solution leak). App rendering a "Watch video solution" link is optional/future APK polish; backend serves it now.
- Ship: backend `./deploy.sh` (content baked + answer_min/max & numeric_correct & video_url are code) — daily quiz auto-upgrades per level on deploy, **no APK rebuild needed**. **NEXT:** the remaining "0 resolved" proof topics are already in the Library books; future = sheet L3/L4 session-course content → our L2/L3 + ISI/CMI → L7.

## REVERSAL — image questions pulled from bank; books only; convert to typed next (2026-06-22)
- **Founder tested on device and called it:** the image-based "Solve the problem shown" Vedantu questions look unfinished anywhere except inside a book. **REMOVED all 1,186 from the served bank** → gone from practice, adaptive ladder, **daily quiz, and contest**. They live **only in the Library faithful-render books** now (content-books/ untouched). Kept the **27 typed GCD&LCM + Cyclicity** (proper LaTeX) as the seed of the target format. Bank 20,828→**19,642** (L4 1275→800, L5 927, L6 800). 126 topic files emptied in place (can't rm on the mount) with a `pending_conversion` marker; **backlog = `content-live/olympiad/qa-reports/CONVERSION_BACKLOG.json`** (126 topics, source label, image-qs count). The reusable `outputs/vedantu_build/ingest_quiz.py` can re-extract any topic's images+keys from the source PDFs on demand for the conversion work.
- **9 OLYMPIAD STRANDS now in the backend taxonomy** (founder-specified subtopics for converted content; founder added Geometry + Arithmetic back): `OLYMPIAD_STRANDS` in `content_store_level.py` = Algebra(ALG) · Number Theory(NT) · Combinatorics(COM) · **Geometry(GEO)** · **Combinatorial Geometry(CGEO)** · Trigonometry(TRIG) · Basic Mathematics(BMATH) · **Arithmetic(ARITH)** · **Algebra-Number Theory(ALGNT)**. `PILLAR_NAMES` rebuilt from it (all 9 first-class now). Exposed via new **`GET /v3/olympiad/strands`**. Both plain Geometry AND Combinatorial Geometry exist (distinct strands), plus the Algebra-Number Theory crossover.
- **NEXT (founder: "we can be slow, only 100% certain"):** convert the backlog's image questions → **typed HTML/LaTeX** in our native question schema (real `stem` with `$…$`, real `choices`, typed `correct_value`/index), tag into the 7 strands, re-populate the emptied topic files (or new ones). Only ingest a question when its transcription + key are 100% certain (the source PDFs + per-problem video links remain the reference). These typed questions then re-enter practice/quiz/contest properly.
- Verified: image topics in practice = 0 (L4/L5/L6), daily quiz still serves 8 (machine pool + the 27 typed NT), books still served (store 21/21). Re-clustered (integrity 0), counts_report/smoke (19,642)/pre_deploy updated. **GREEN: v3 17/17 · contest 23/23 · store 21/21 · adaptive 18/18 · pre_deploy PASS.** Ship: backend `./deploy.sh`.

## L3/L4 grade-course workbooks (2026-06-22)
- Founder: download sheet L3+L4 (the not-yet-downloaded session courses) + make them **Library books** (chose faithful-render books over typed-into-bank, since L2/L3 are already populated). Verified the local L5/L6/L7 folders = sheet L5/L6/L7 (read sheet Level 5 → exact topic match with local L5; founder's "only L6/L7 downloaded" was off — L5 was already on disk). Genuinely missing = sheet L3 (G3-4) + L4 (G5-6).
- **DOWNLOAD**: read the L3+L4 index sheets via Drive connector, generated `download_vedantu_L3L4.py` (65 assignment PDFs, parsed file IDs from the sheets). **Bug fixed**: rclone command is `rclone backend copyid REMOTE id dir/` (a drive *backend* command + temp-dir-then-move), NOT `rclone copyid` (which errors "unknown command"). Founder ran it → 65 PDFs in `~/Downloads/Vedantu_Content/L3` (32) + `/L4` (33).
- **ORIGINAL AUTHORED L2 TEACHING BOOK — "Kiwi's Grand Math Adventure" (2026-06-22)**: founder asked to *write a real book* (not faithful render) for L2 (Grade 3-4): "go through each question/subtopic/topic, bring all maths together, easy kid language, Socratic + Bloom's, draw exact SVG figures, use OUR taxonomy names, start G3 basics → highest level, after reading the kid can solve everything." Built an HTML book authored from scratch in `outputs/l2book/` (a Python builder: `build_l2book.py` + shared `l2_helpers.py`/`l2_figs.py` = a ~25-function exact-vector SVG toolkit: base-ten, place-value, number line, fraction bar/circle, decimal grid, factor tree, array dots, area grid, polygons, 3D solids, cube net, symmetry, venn, bar/pie/pictograph, magic square, clock, spinner, balance, pattern). One module per chapter `l2_chNN_*.py` (chapter(part,num,title,taxonomy,body)). Pedagogy per chapter = big-question hook → Kiwi-mascot Socratic build → worked examples → inline try-its → **Bloom's ladder** practice (Remember→Understand→Apply→Analyze→Create, answers in tap-reveals) → ⭐ challenge. Organised by our L2 taxonomy friendly names (Big Numbers, Number Families, Fair Shares, Rule Finders, Balance the Scale, Measure Masters, Turn & Flip, Brain Benders, Data Detectives). **22 chapters across 7 parts, 152 SVG figures, ~600 questions, 0.54MB.** Founder add-on: **"cover Kangaroo past questions too"** → studied our Kangaroo-derived bank (content-v2 topic-*) + added **Part 7 · 🦘 Kangaroo Corner** (Ch21 "Kangaroo Thinking Tricks" teaches cuts-vs-pieces / fold-and-cut / cube-stacks / grid-paths / product-sum digit puzzles / logical-elimination / working-backwards; Ch22 graded 3·4·5-point MCQ set, 5-option A–E, worked solutions). Built mostly via 7 parallel subagents (each brute-force-verified every answer in Python + render-tested figures via cairosvg; I QA'd centrally — well-formed tags, cross-section figure render, fixed toolkit bugs: fraction-bar empty-cell black, area-grid faint lines, venn contrast, balance labels). Reuses the collapsible-card + pure-anchor nav (works without JS). Registered in `store_service` (`l2-mathbook`, "Kiwi's Grand Math Adventure", subject "Math Book", levels [2], free); smoke_store 24→**25 books**, claims+serves 200. Ship: backend `./deploy.sh` (bakes content-books + catalog). NEXT (optional): painted catalog cover for it; same authored-book treatment for L1/L3.
- **BUILT 2 faithful-render workbooks** (`outputs/vedantu_build/build_workbook.py` — flat-folder variant of build_book.py for session courses: mixed topics, session-ordered, worked-solution reveal, NO video tab since these have none, general gold **magic-square cover motif**, distinct deep palettes G3-4 green / G5-6 indigo): `content-books/g34-workbook/` (Grade 3–4, 32 sessions, 187 pages, 12.5MB) + `content-books/g56-workbook/` (Grade 5–6, 33 sessions, 216 pages, 14.2MB). Render verified clean (figures/tables/MCQ options/iOM provenance intact). Registered in `store_service` (_BOOK_FILES + 2 catalog rows, subject "Olympiad Workbook", levels [2]/[3], free). smoke_store updated 22→**24 books**, both claim+serve 200. Ship: backend `./deploy.sh` (bakes content-books + catalog) + APK lists them dynamically. **NEXT (when founder wants): ISI/CMI sheet → L7/L8 (separate), + the L5 chapter-assignments/reference books were skipped.**

## Progress per-grade bug + points clarity + book covers (2026-06-22)
- **REAL BUG FIXED — progress score didn't change with grade.** Root cause: `/v3/me/progress` took only `user_id` and derived the scale score from GLOBAL `state.accuracy_percent`/`topics_mastered_count`, so the Parent dashboard labelled it "…for Grade X" while the number was identical for every grade. Fix: `me_progress` now accepts optional `level=` and scopes accuracy/mastery/strands to that level's topics (new `level_store.topic_in_level(tk, level)`); returns `scope` + `scope_attempts`. Flutter `getProgress(userId, {level})`; Parent `_dashboard()` passes grade→level (`L${(grade+1)~/2}`) and shows "No Grade X practice yet" when `scope_attempts==0`. Verified: overall 440 · L6(practiced) 440 · L1(unpracticed) honest no-data → **score changes by level = True**, smoke 17/17, Dart balanced. Ship: ./deploy.sh + APK.
- **POINTS/ECONOMY clarified** (`KIWIMATH_POINTS_AND_ECONOMY.md`): v3 wallet is already clean — **Coins/Gems/XP/Streak** + **LP/Rating**; the old "two gem meters" is gone. Doc maps each ("XP & Rating measure you, Coins & Gems reward you, Streak & LP keep you coming back; money never buys rank"), the one-ledger connection, and the Library→Quiz flow (books + Verified daily-quiz pool). Recommend: kid Profile shows XP/Streak/Coins/Gems/Rating w/ plain-English subtitles; parent view shows no currency (already true); fold any legacy engagement-gems into the single Gems.
- **BOOK COVERS:** added a tier badge (IOQM/RMO/INMO/Ln from `book.levels`) to `BookCoverArt` so per-pillar-per-level books ("RMO Number Theory" vs "IOQM Number Theory") read apart at a glance; painted covers otherwise unchanged. Full faithful-render book covers come with the book build (awaiting synced PDF folder).
- **STATE+ROADMAP doc** written (`KIWIMATH_STATE_AND_ROADMAP.md`): full app summary + expert roadmap (converge→ship core to Vedantu students→instrument→content moat→safe monetization→scale).

## Leaderboard / Economy design (2026-06-18, design phase — not built yet)
- App repositioning: **mostly Grade 6–10, olympiad-leaning** (L3–L7 the core; L1–L2 younger). Leaderboard designed for this.
- **`KIWIMATH_LEADERBOARD_DESIGN.md`** — full plan: flagship **Daily Contest** (fixed 6 PM IST, biggest points, one attempt) + Daily/Weekly-League/Monthly-Season boards + persistent **Kiwi Rating** (chess/Elo-style, contests-only, grade 6–10 magnet). **One engine, 3 presentation tiers** (Junior L1–2 gentle/no-rating · Middle L3–4 · Senior L5–7 full competitive); matchmaking within-level. Point stack: **LP** (weekly-reset rank) · Rating (persistent) · Coins/Gems (spendable) · XP/Streak — one server ledger, "no disjoint". 12 engagement hooks + wellbeing guardrails. Mockup `kiwimath_leaderboard_mockup.html` (6 phone screens).
- **`KIWIMATH_ECONOMY_STORE_CONTRACT.md`** — cross-cowork-space contract for the **future bookstore + reader (being built in ANOTHER cowork space)**: one shared wallet owned by Economy (Coins/Gems), bookstore owns catalog+entitlements; `getBalance/spend/grant` API (server-authoritative, idempotent), purchase **saga** + reconciliation, dual earn-OR-buy book pricing (common=Coins ~1.5–3k ≈ 2 wks; premium=Gems/money), no pay-to-win (money never buys rank), **consolidate the two gem meters into one Gems**.
- **LEADERBOARD MVP BACKEND BUILT 2026-06-18** (`qa-reports/LEADERBOARD_MVP_BUILD_2026-06-18.md`): Daily Contest + Weekly League + LP. New services `backend/app/services/contest_service.py` (deterministic 8-q set per date+level from level_store, server-grade, score=base×correct×speed×streak×ontime, awards economy via gamification.record_answer + LP via league, **one-attempt replay/no-double-award, no answer leak**) + `league_service.py` (cohorts ~30, tiers Bronze→Legendary, add_lp/standings+zones/rollover top7-promote-bottom7-relegate, FirestoreBackedStore). API `backend/app/api/contest.py` (`/v3/contest/today`,`/contest/submit`,`/contest/leaderboard`,`/v3/league/me`; assert_user_match→403 cross-user) wired in main.py. answer/check now adds +5 practice LP. Test `tests/smoke_contest_league.py` **21/21**; v3 17/17 + adaptive 18/18 + pre_deploy green + scanner 0. Contest opens 6PM IST (KIWIMATH_CONTEST_ALWAYS_OPEN=1 for test). **Rollover DONE**: `POST /v3/internal/league-rollover` (X-Internal-Key like clan cron → verify_token internal identity; normal user 403; league weeks IST-aligned) + `backend/deploy/league_cron.yaml` (Sun 23:55 IST). **Flutter DONE (needs APK build)**: `app/lib/services/contest_service.dart` (client) + `app/lib/v3/contest_screens.dart` (CompeteHubScreen/DailyContestScreen lobby→timed quiz→results+board with MathText+svg/png, LeagueScreen cohort board w/ promote-relegate stripes) + wired "Compete" banner into OlympiadTab in kiwi_v3.dart (structural-check balanced). smoke_contest_league **23/23** + v3 17/17 + adaptive 18/18 + pre_deploy green. **REMAINING:** APK rebuild (-t lib/main_v3.dart) · apply league_cron via gcloud after deploy · V2 Rating/Seasons/cosmetics + age-tiered presentation. Ship backend ./deploy.sh.

## Store / Reader (KiwiReader integration — from another cowork space)
- KiwiReader = built+tested Flutter package (`kiwi_reader` + pure-Dart `kiwi_reader_core`, 89 tests) giving Reader (HTML/PDF/EPUB+annotations) + Library (offline) + Store (buy money / unlock coins). Drops in via **host seams** (6 adapters) + Riverpod ProviderScope overrides. We write NO reader logic. Its `CoinWallet.spend(amount,reason,bookId)` ≡ our Economy Contract `spend`. Source now in repo at **`kiwimath/kiwireader/`** (548K, clean, no .dart_tool). Docs: HANDOVER_KIWIMATH.md, AUDIT.md (O1 purchase-durability/O2 encrypt-at-rest/O3 purge-on-signout = host must-dos), openapi.yaml (sync).
- Plan: **`KIWIMATH_STORE_INTEGRATION_PLAN.md`** — 6 adapters (CoinWallet→/v1/economy/spend, CatalogProvider→/v3/store/catalog, ContentProvider→/v3/store/content, AuthProvider→Firebase, PurchaseGateway→IAP, EntitlementStore) + backend (catalog/content/entitlements/sync/economy spend+grant) + 4 launch must-dos. Gems ride the GRANT path (milestone book gifts) for MVP; coins via CoinWallet, money via IAP.
- **PHASE 0 SPIKE WIRED 2026-06-19 (needs flutter pub get + run)**: `app/lib/v3/books_integration.dart` (DEV in-memory adapters _DevCatalog/_DevContent/_DevWallet(500 coins)/_DevGateway + sample HTML book + 5-book catalog + `booksOverrides()` + `BooksHubScreen` Store/Library tabs). `app/pubspec.yaml`: added `flutter_riverpod ^2.5.1` + `kiwi_reader` (path ../kiwireader/packages/kiwi_reader), SDK bumped 3.3→3.4. `app/lib/main_v3.dart`: wrapped root in `ProviderScope(overrides: booksOverrides())` — existing orange MaterialApp theme (colorSchemeSeed 0xFFFF6D00) flows into KiwiReader screens = color scheme combined for free. `kiwi_v3.dart`: "Library" banner on Olympiad tab → BooksHubScreen. All 9 overridden providers + screen classes verified to exist in package; Dart balanced. **Caveat:** kiwi_reader pulls pdfrx + flutter_epub_viewer + sqflite + connectivity_plus → may need Android minSdk≥21 / iOS≥12 bump on `flutter pub get`. Spike uses HtmlRenderer so PDF/EPUB native not exercised at runtime. NEXT: Phase 1 (real adapters + economy spend/wallet + catalog/entitlements backend).
- **STORE PHASE 1 DONE 2026-06-19** (`qa-reports/STORE_PHASE1_BUILD_2026-06-19.md`): coins end-to-end. Backend: `gamification.spend/grant` (authoritative debit/credit on kiwi_coins/gems) + `economy_service.py` (wallet/spend[idempotent, reason=unlock_book records entitlement = O1 owns-at-debit, txn ledger]/grant/claim_free) + `store_service.py` (seeded 5-book catalog CatalogBook-shape + per-user entitlements FirestoreBackedStore, no-pricing=auto-owned) + `api/store.py` (`/v3/store/catalog`,`/v3/store/entitlements`,`/v3/economy/wallet`,`POST /v3/economy/spend`[price enforced server-side],`/v3/store/claim`[free only],`/v3/economy/grant`[admin/internal]) wired in main.py. App: production adapters in books_integration.dart (_BackendCatalog/_BackendWallet[stable idem key uid:book:reason]/_BackendEntitlements[load=reconcile on launch, save=free-claim push]) + `backendBooksOverrides()`; main_v3 switched to it. **smoke_store 18/18** + v3 17/17 + adaptive 18/18 + contest 23/23 + pre_deploy green; 276 routes. STUBBED (by design): content=HTML sample (real bytes need ingestion→GCS, Phase 1.5), money=dev gateway (IAP=Phase 2), sync=in-memory (Phase 2), O2 encrypt/O3 purge=Phase 3, leaderboard book-gift wiring=Phase 4 (grant endpoint ready). Ship: backend ./deploy.sh + app flutter build -t lib/main_v3.dart.
- **STORE PHASE 1.5 (real book ingestion) DONE 2026-06-19** (`qa-reports/STORE_PHASE1_5_BOOK_INGEST_2026-06-19.md`): first real book served end-to-end — **Euclid's Garden** (Anand Prakash, 30 ch, 81 figs) as **EPUB** (KiwiReader-native EpubRenderer, reflows; chose EPUB over the book's `reader/` HTML folder since KiwiReader's HTML path wants structured HtmlBook JSON not raw HTML; PDF also baked as 1-line-swap fallback). Files at `content-books/euclids-garden/` (EuclidsGarden.epub 648KB valid EPUB3 + Book.pdf + generated cover.png). Backend: `store_service` catalog row (school-issued=no-pricing→auto-owned→readable now) + `_BOOK_FILES` map under `KIWIMATH_BOOKS_DIR`; `api/store.py` +3 routes `GET /v3/store/content/{id}/manifest|bytes|cover` (manifest+bytes **entitlement-gated** via decoded uid + store.is_owned; bytes=FileResponse w/ Range; cover open to signed-in). `deploy.sh` bakes `content-books/`→`/content-books` + sets `KIWIMATH_BOOKS_DIR`. App `books_integration.dart`: real `_BackendContent` ContentProvider (manifest/bytes from endpoints via authed_http.bodyBytes) + `_openBook` now builds renderer **per book format** (EpubRenderer.open/PdfRenderer.open/HtmlRenderer) from the active ContentProvider, loading spinner, pushes KiwiReader in a nested `ProviderScope` overriding `contentRendererProvider` (KiwiReader READS that provider — kiwi_reader_widget line43 `ref.read(contentRendererProvider)` — it does NOT auto-resolve from format); `backendBooksOverrides` now uses `_BackendContent`. smoke_store **24/24** (+manifest/bytes/cover/404), 279 routes, v3 17/17 + adaptive 18/18 + contest 23/23 + pre_deploy green + scanner 0, dart balanced. **Device-only**: EPUB(epub.js WebView)/PDF(PDFium) can't render in sandbox → on-device QA needed (open book, check figs/math/reflow; if EPUB off, flip catalog row to format:pdf, no code change). Cover not yet wired as coverUrl (auth-on-image → placeholder card; move covers to public URL/GCS later). Ship: backend ./deploy.sh + APK flutter build -t lib/main_v3.dart.
- **READER COMPILE FIXES + first APK with KiwiReader (2026-06-19)**: compiling the app pulled in KiwiReader's Flutter layer for the FIRST time (prior APK predated store) → surfaced 2 latent package bugs: `kiwi_reader_widget.dart` called undefined `_createExternal` (×4, real method = `_createPdf`) + `epub_reader_surface.dart` used nonexistent `EpubSource.fromBytes` (1.2.8 loads url/file/asset → now stages bytes to a temp file + `EpubSource.fromFile`). Then exhaustive pass: scripted scan for undefined-private-methods across all 28 files (0), external-API verify vs resolved versions (pdfrx 1.3.5 PdfViewer.data/PdfViewerParams/openData/paint-callback all confirmed via dartdoc; sqflite/connectivity_plus 6.x/uuid OK), subagent cross-check of every ctor/override/enum-switch vs core (0 more). **APK built green (79.3MB).** Fast loop = `cd kiwireader/packages/kiwi_reader && flutter analyze` (8s, app-dir analyze does NOT descend into path-package).
- **READER UX v2 from live feedback (2026-06-19, needs rebuild+device QA)**: 7 fixes. (1) **Library = bottom-nav tab** now (5 tabs: Olympiad/School/Library/Progress/Profile) via new `BooksHubBody` (no-Scaffold body, shell supplies AppBar+wallet) in books_integration.dart; removed Olympiad `_booksBanner`. (2) **Colors → Kiwimath orange**: KiwiReader was green/teal — `_kGreen` in store_screen+library_screen → `0xFFFF6F00`, book_cover gradient teal→orange, selection_toolbar bg→`colorScheme.inverseSurface`, epub settings/chips use `cs.primary`. (3) **Font size** control: `_controller.setFontSize(fontSize: int px)` (README API; px not %, applied on-demand not load so book always natural by default; default 18, range 12–40 step 2). (4) **Scroll mode** toggle Pages/Scroll: `setFlow(EpubFlow.paginated|scrolled)` + `EpubDisplaySettings(flow,snap,useSnapAnimationAndroid:false,theme:EpubTheme.light(),allowScriptedContent:true)`. (5) smoothness = passing proper displaySettings + scrolled flow option. (6) **Highlight toolbar near selection** not bottom: `onTouchUp:(x,y)` normalized → position via LayoutBuilder (was Positioned bottom:28). (7) **SafeArea** wraps reader body (Android nav-bar no longer covers page). Reading-settings sheet = `Icons.text_fields` button top-right of EPUB surface (font −/＋ + Pages/Scroll chips). flutter_epub_viewer API all from pub.dev README/example (repo fayis672/epub_viewer, v1.2.8). **WATCH on device**: setFontSize px-vs-% (if +/- feels off, flip unit/range — 1-line); onTouchUp toolbar coords mapping. Files: kiwireader epub_reader_surface.dart/kiwi_reader_widget.dart/store_screen.dart/library_screen.dart/book_cover.dart/selection_toolbar.dart + app books_integration.dart/kiwi_v3.dart. All delimiter-balanced. Verify: `cd kiwireader/packages/kiwi_reader && flutter analyze` then app build.
- **OWNED BOOKS UI — painted covers + grade/level filter (2026-06-19, needs rebuild+device QA)**: per founder ask ("recreate it yourself, don't rely on breaking external API, add filter + beautiful covers"), the entire book-browsing UI is now OUR Dart code, NOT KiwiReader's StoreScreen/LibraryScreen. New `app/lib/v3/books_browse.dart`: `BooksService` (direct backend client — catalog/entitlements/wallet/spend/claim, no KiwiReader providers), **`BookCoverArt`** = hand-painted premium covers via CustomPainter (per-subject deep base + gold motif: Geometry=circle+triangle, Algebra=parabola+axes, NumberTheory=dot-lattice, Combinatorics=graph, StudySkills=open-book; cream serif title/author — zero network/asset dependency), **`BooksBrowseBody`** = Store + My-books grids with a **Filter button** (Level L1–8 + Subject multi-select chips, active-filter chips) + unlock-with-coins sheet + open. Decision stated to founder: NOT reimplementing the EPUB engine (epub.js) — that adds bugs; instead own everything around it; the ONE hardened boundary to the engine = new public `openBookReader(context, bookId)` in books_integration.dart (manifest→renderer→KiwiReader, snackbar on any failure, never crashes). `_openBook(CatalogBook)` now delegates to it. kiwi_v3 Library tab → `BooksBrowseBody` (dropped BooksHubBody + books_integration import). Backend `store_service`: catalog books gained `subject`/`levels[]`/`gradeBand`; **+6 coming-soon titles** (no content, `comingSoon:True` → not auto-owned/claimable/ownable — guarded in is_free/own) so the shelf looks full + filter is meaningful (12 books total). smoke_store **26/26** (catalog 12 + coming-soon-not-claimable). All Dart delimiter-balanced; app-dir `flutter analyze` WILL check books_browse.dart (it's app code, not the path-package). Ship: backend ./deploy.sh + APK build. Device QA: covers/filter/unlock/open + reader UX v2 items.
- **READER NAV REDESIGN — root-caused the no-flip + Kindle tap-zones (2026-06-19, needs rebuild+device QA)**: researched epub.js model (futurepress docs + flutter_epub_viewer 1.2.8 dartdoc — note library segment is `epub_viewer` not `flutter_epub_viewer`, that's why earlier class-page fetches 404'd). **ROOT CAUSE of pages-not-flipping found**: `EpubDisplaySettings.manager` **defaults to `EpubManager.continuous`**, but epub.js pairs paginated→`defaultManager` / scrolled→`continuous` ("wrong combo looks weird/breaks paging"). We never set manager → paginated ran on continuous → no page turns. FIX: `manager: _flow==paginated ? EpubManager.defaultManager : EpubManager.continuous` in displaySettings (+ kept StackFit.expand for definite viewport, + `fontSize:_fontPx` int in displaySettings — constructor fontSize is int, runtime setFontSize is double). **Kindle tap-nav from scratch**: NOT a Flutter gesture layer over the WebView (would eat selection) — instead the viewer's own `onTouchDown`/`onTouchUp` (normalized coords): a still(<0.04 move)+quick(<350ms) tap with no active selection in left third → `_controller.prev()`, right third → `_controller.next()`, centre = reading. Scroll mode: taps disabled (you scroll). Added `onRelocated`→thin bottom `LinearProgressIndicator` (loc.progress), one-time auto-dismiss tap-hint overlay (IgnorePointer, prefs `kiwi_reader.tapHint`). EpubDisplaySettings full ctor (verified): `fontSize:int=15, spread=EpubSpread.auto, flow=paginated, allowScriptedContent=false, defaultDirection=ltr, snap=true, useSnapAnimationAndroid=false, manager=EpubManager.continuous, theme`. Controller verified: next()/prev()/setFlow/setManager/setFontSize(double)/setSpread/clearSelection. Only epub_reader_surface.dart changed; balanced. Ship: APK build (`cd app && flutter pub get && flutter build apk --release -t lib/main_v3.dart`).
- **READER FLICKER + SCROLL-JANK FIX (2026-06-20)**: live feedback "flickering after open, scroll not smooth". Root cause = `onRelocated` called `setState` on every page-turn/scroll-tick to update the progress bar → rebuilt the whole `EpubViewer` (new displaySettings instance each time) → epub.js reflow → flicker; in scroll mode onRelocated fires continuously → setState storm → jank. Fix (epub_reader_surface.dart): (1) **cache the viewer** — `Widget? _viewer; _viewer ??= _buildViewer();` built ONCE, reused every rebuild (font/layout still change live via controller, never by re-creating the WebView). (2) **progress via `ValueNotifier<double>` + `ValueListenableBuilder`** — `onRelocated:(loc)=>_progress.value=loc.progress` (NO setState) so paging/scrolling only repaints the 2.5px bar, never the WebView. (3) `_applySettings` now **diff-guards** (only setFlow/setFontSize when `_flow/_fontPx != _bakedFlow/_bakedFont`) → no redundant reflow on load. (4) `_applyFont/_applyFlow` dropped their main-tree setState (controller applies; sheet uses its own setSheet). API all verified vs pub-cache source last round (next/prev/setFlow/setFontSize(double) confirmed; EpubManager has only `continuous` so manager left default). Balanced. Ship: `cd app && flutter build apk --release -t lib/main_v3.dart`.
- **IMMERSIVE KINDLE-STYLE READER — full rewrite as OUR app code (2026-06-20, needs build+device QA)**: founder compared our reader (clumsy floating "Tt", no chapter list / page-jump, always-on cloud/list app bar) to Kindle (immersive: tap centre reveals top+bottom bars, contents, scrubber). Built a brand-new reader **in the app** directly on flutter_epub_viewer, bypassing KiwiReader's reader entirely. New `app/lib/v3/book_reader.dart` `BookReaderScreen(bookId,title)`: fetches bytes from `/v3/store/content/{id}/bytes` → temp file → EpubViewer; **immersive** (page = full screen, `_chrome` bool, tap-centre shows/hides top+bottom bars via AnimatedPositioned); top bar = back · title · contents · **Aa**; bottom bar = chapters · **scrubber Slider**(→`toProgressPercentage`) · %; tap left/right thirds → `prev()/next()`, any tap while chrome up hides it; **contents list** from `onChaptersLoaded`→flattened EpubChapter(title/href/subitems), tap→`display(cfi: href)`; **Aa sheet** = font −/＋ (`setFontSize` double), **theme Light/Sepia/Dark** (`EpubTheme.light()` + `EpubTheme.custom(customCss:{...})`), layout Pages/Scroll; **precise CFI resume** — save `loc.startCfi` per book (`kiwi_reader.cfi.<id>`) throttled + on dispose, restore via `display(cfi:)` in `onLocationLoaded`; flow/theme change **re-creates** the viewer (ValueKey `epub-flow-theme`) keeping place; viewer cached (`_viewer ??=`) + progress via ValueNotifier (no flicker). Added `flutter_epub_viewer: ^1.2.8` to app pubspec. `books_browse.dart` now pushes `BookReaderScreen` (dropped `openBookReader` import). **ALL epub APIs confirmed vs pub-cache source** (EpubChapter{title,href,id,subitems}, EpubLocation{startCfi,endCfi,progress}, getChapters()→List, display({cfi}), toProgressPercentage(double), onChaptersLoaded/onLocationLoaded/onRelocated/onTouchUp). **ONE watch-item**: `EpubTheme.custom(customCss:)` is from the README (constructor — README constructors have held up, only the EpubManager enum value was ever wrong); if build errors on it, fall back to light-only or fix param. Both files delimiter-balanced. KiwiReader package now fully unused for reading (store/library already ours) — could drop the dep later. Ship: `cd app && flutter pub get && flutter build apk --release -t lib/main_v3.dart`.
- **READER — purpose-driven redesign + batched (2026-06-20)**: founder pushed back ("don't ship a substandard Kindle clone — ask WHY each element exists"). Reasoned it out: ours is a *study* reader (math textbook, figures, non-linear) not a novel reader → top bar = identity+global tools, bottom bar = position+movement (the why behind Kindle's title-top/chapter-bottom split). Agreed plan: full work in **batches, core-first**. **Batch 1 DONE** (`book_reader.dart`): **"Back to my spot" pill** (`_offerReturn`/`_jumpTo`/`_goBack` — save `_curCfi` before any contents/scrubber jump, dark pill 25s, `display(cfi:)` to return) + **current chapter highlighted** in contents (`_navChapterId`). **Batch 2 DONE**: **margins** (Narrow/Normal/Wide → body padding-left/right px) + **line spacing** (Compact/Normal/Relaxed → line-height) via unified `EpubTheme.custom(customCss:)` for ALL themes (record-destructure `(bg,fg,head,link)` per theme + `const [12,26,46][_marginLevel]` / `const ['1.4','1.65','2.0'][_lineLevel]`); `_recreate()` shared by setFlow/setTheme/setMargin/setLine (ValueKey now `epub-flow-theme-margin-line`, CFI-preserving); Aa sheet got Margins + Line-spacing seg-rows (`_segRow`) wrapped in SingleChildScrollView; **bottom bar restructured** to 2 rows: chapter name (`_navChapterTitle` from `_navChapterId`, book-title fallback) + % on top, chapters-icon + scrubber below. `EpubTheme.custom`+`customCss:Map<String,dynamic>` grep-confirmed. **API LIMIT found**: flutter_epub_viewer EpubLocation exposes only startCfi/endCfi/progress — NO current-chapter; so live chapter name during *free* reading isn't available (only the last-opened chapter is shown — correct for the navigate-then-read study flow). **Batch 3 TODO**: search (`controller.search`), tap-figure-to-enlarge (needs JS-injection investigation — package doesn't expose image taps), and true live-chapter/scrubber-ticks (need a CFI→chapter spine-index mapping, validate on device). **WATCH on device**: body `padding-left/right` may not move margins in epub.js paginated columns (line-height is reliable) — if margins don't visibly change, switch approach. Ship: `cd app && flutter pub get && flutter build apk --release -t lib/main_v3.dart`.
- **READER "why"-review polish (2026-06-20)**: founder asked to re-review every element by purpose before Batch 3. Found + fixed 3: (1) **true immersion** — `SystemChrome.setEnabledSystemUIMode(immersiveSticky)` hides Android status/nav bars while reading, `manual`+all overlays when chrome shown / on dispose (via `_setChrome(bool)` + `_applyImmersion(reading:)`, `flutter/services` import); the page now fills the screen. (2) **loading veil** — `_ready` flag set in `onEpubLoaded` shows a spinner on `_pageBg` until epub.js paints (no blank flash on open). (3) **removed redundant bottom chapters icon** (top-bar Contents already covers it; scrubber gets the room). Rest of the reader judged purpose-clean. book_reader.dart balanced, no new deps. Build: `cd app && flutter build apk --release -t lib/main_v3.dart`. NEXT = Batch 3 (search + tap-figure-to-enlarge + CFI→chapter mapping for live chapter/ticks).
- **READER BATCH 3 — search shipped; figure-zoom + live-chapter deferred (2026-06-20)**: **Search DONE** — top-bar search icon → `_openSearch` pushes `_SearchScreen` (own Flutter: autofocus TextField + results ListView, empty/loading/no-result states) → `_controller.search(query:)` **grep-confirmed** `Future<List<EpubSearchResult>> search({required String query})`, `EpubSearchResult{String cfi; String excerpt; String? xpath}` → maps to `_SearchHit(cfi,excerpt)`, tap → `Navigator.pop(cfi)` → `_jumpTo(cfi)` (gets the back-to-spot pill). Immersion/comfort build shipped 77.1MB before this. **Deferred with reasons (engine limits, NOT shipping flaky)**: (1) **tap-figure-to-enlarge** — flutter_epub_viewer exposes only normalized x/y on touch (not the tapped element) + no JS-injection hook → can't detect/zoom a figure; real fix = serve figures as separate images + overlay tap-zones, or a different renderer (future project). (2) **live chapter name + scrubber ticks** — EpubLocation carries only cfi+progress (no chapter ref) + no chapter→progress map; reliable mapping would need silently probing every chapter at open (visibly jumpy) → kept "last-opened chapter" (`_navChapterId`, correct for navigate-then-read). book_reader.dart balanced. Ship: `cd app && flutter build apk --release -t lib/main_v3.dart`.
- **FULLY-OWNED HTML READER — Batch A (2026-06-20, needs build+device QA)**: founder's call after the epub.js reader stayed jerky ("Build a reader we fully own"). Root cause recap: flutter_epub_viewer's `EpubManager` enum exposes ONLY `continuous` (the wrong manager for paginated), and scrolled flow is epub.js's own weak spot → jerkiness is an ENGINE limit, not our code. Fix = render the book's bundled **self-contained HTML edition** in a WebView we fully control (native-smooth scroll, our chrome). **Backend**: `euclids-garden` flipped EPUB→**HTML** — `store_service._BOOK_FILES` + catalog row now `format:html`, file `euclids-garden/EuclidsGarden_Mobile.html` (1,926,329 B, copied into content-books/, ships via deploy.sh `KIWIMATH_BOOKS_DIR`). `/v3/store/content/{id}/bytes` serves it `text/html` (entitlement-gated, unchanged). smoke_store updated (manifest/bytes/catalog assert html) → **26/26**. **App**: new `app/lib/v3/html_book_reader.dart` `HtmlBookReader(bookId,title)` — fetches bytes via authed_http → `utf8.decode` → renders with **flutter_inappwebview** `InAppWebView(initialData: InAppWebViewInitialData(data:_html, baseUrl:https://reader.kiwimath.app/))` (string load = no deprecated/insecure `file://` flags; baseUrl gives a real origin so the book's inline script's localStorage doesn't throw). Drives theme/font through the book's OWN CSS hooks via injected JS: dark=`body.classList.add('night')`, sepia=override `--bg/--fg/--card`, font=`--fscale` (70–220%); hides the book's `.bar` (we supply chrome). Immersive (`SystemUiMode.immersiveSticky`); tap-to-toggle chrome via an injected `click` listener → `callHandler('ktap')` (skips taps on `<a>` so internal links still navigate); progress + scroll-resume via a throttled JS scroll listener → `callHandler('kscroll', frac)` (ValueNotifier, no setState storm) + `window.scrollTo` restore; per-book pos + font + theme persisted in SharedPreferences. pubspec += `flutter_inappwebview ^6.1.5` (already transitive via epub viewer). API verified via web search (InAppWebViewSettings/WebUri/addJavaScriptHandler/initialData all confirmed). `books_browse._open` now routes `b.format=='html'`→HtmlBookReader else BookReaderScreen (EPUB/PDF). Both Dart files delimiter-balanced; epub reader (book_reader.dart) untouched & still used for non-HTML. **Batch B TODO**: contents list (TOC from `id="chNN"`/`#contents` anchors → scrollIntoView), in-page search (JS find), pinch-zoom figure affordance. Ship: backend ./deploy.sh + `cd app && flutter pub get && flutter build apk --release -t lib/main_v3.dart`. Device QA: smooth scroll, tap-chrome, font/theme persist, resume-on-reopen.
- **HTML READER — font/paged/zoom fixes (2026-06-20, needs build+device QA)**: live feedback on the owned HTML reader: (1) want continuous-scroll OR flip-like-book, (2) font won't change, (3) pinch-zoom "doesn't adjust to screen". **Inspected the book's actual CSS/markup** (didn't guess): root cause of #2 = **the book has NO `<main>` element**, so its `main{font-size:calc(1.0625rem*var(--fscale))}` rule styles nothing and `--fscale` was a no-op; content lives in `<section id="chNN">` (30) + `<div class="lib" id="contents">`, body has no font-size so it inherits 16px from `<html>`. **Fix #2**: scale the root `<html>` font-size (`documentElement.style.fontSize='{pct}%'`) — book is fully rem/em-based so everything scales; 70–220%. **Fix #1 (paged/flip)**: rewrote the in-page logic as one injected `window.__kiwi` controller + a `<style>` (`html.kpaged body{column-width:calc(100vw-44px);column-gap:44px;column-fill:auto;transition:transform .26s}` + `padding:0 22px`, zero section L/R pad, `break-inside:avoid` on figure/svg/img) — paged mode lays body out as full-width CSS columns and slides it with `translateX(-page*100vw)` (Kindle side-tap zones: <30%=prev, >72%=next, centre=chrome; scroll mode any tap=chrome). `count()=ceil((scrollWidth-1)/innerWidth)` so the last page never clips. One source of truth = a 0..1 `frac` (scroll mode: scrollY/(scrollH-innerH); paged: cur/(n-1)) → `kprogress` handler → ValueNotifier (no flicker) + throttled save; switching modes/font re-derives position from `frac` so place is kept. **Fix #3**: added `useWideViewPort:true`+`loadWithOverviewMode:true` so the width=device-width viewport lays out + fits, pinch-zoom kept for figures (and once font works they won't need to pinch text). Settings sheet gained a **Scroll/Pages** segmented control (`_modePill`) + font/theme persist incl. `_pagedKey`. APK-only this round (no backend change): `cd app && flutter pub get && flutter build apk --release -t lib/main_v3.dart`. **Device QA**: font +/- now visibly changes text; Pages mode flips on side-tap + remembers mode; pinch-zoom a figure. **Watch (paged geometry, eyeball)**: per-page L/R margins (22px), last-page fill (ceil), and font-change re-layout in paged mode — tune the CSS consts if any feel off. book_reader.dart (EPUB) untouched.
- **IOQM "Four Pillars" → interactive books · GEOMETRY pilot DONE (2026-06-20, needs build+device QA)**: founder's PDFs at `Downloads/IOQM 2026/` (4 pillars: Algebra 12 topics, Combi 8, Geometry 13, NT 8, + loose Identities/Inequalities). Each topic PDF = Problems (Level I/II) → Answers Key → 15 YouTube video-solution links → worked Solutions w/ figures. Ask: convert each pillar to an interactive book **without changing questions**. Decision (founder: "you built the reader, you have context" + "4 books in library" + "geometry first, it has images"): **faithful page renders** (pdftotext mangles the math → re-typing would corrupt questions; so render each page to **WebP 1.7x** via PyMuPDF — pixel-faithful, questions untouched) wrapped in an **interactive HTML shell** for our reader. Built `geometry-ioqm` book (`content-books/geometry-ioqm/EuclidGeometry_IOQM.html`, **12.57 MB**, 165 pages, 13 topics, 195 video links): branded cover + tappable Contents → per topic: Problems shown (1st page eager, rest lazy via IntersectionObserver, `aspect-ratio:.707` so no layout jump), then `<details>` reveals for **Video solutions** (tappable YouTube buttons) and **Answers & worked solutions** (lazy page images). Themeable (`body.night`), CSS-var colors. Build pipeline in `outputs/ioqm_build/` (`assemble.py` + manifest). **Reader upgraded for big books + external links** (`html_book_reader.dart`): now stages bytes→temp file→**file://** load (a 12.5 MB book shouldn't ride the platform channel as one initialData string; only `allowFileAccess`, no cross-origin flags) + **`shouldOverrideUrlLoading`** opens http/https (video) links in the system browser via **url_launcher ^6.3.2** (added to pubspec, was transitive) while in-page `#topic` anchors load in place + click handler now skips `a,button,summary,details,label,input,...` so reveal taps don't toggle chrome/flip pages. Backend: `store_service` +`_BOOK_FILES["geometry-ioqm"]` + catalog row (html, school-issued=no pricing→auto-owned→readable now, subject Geometry, levels [5,6,7]). deploy.sh already bakes `content-books/`. smoke_store updated 12→**13 books**, asserts geometry-ioqm html/owned → all PASS; content endpoint serves 12,567,209 B text/html with details+video+youtu intact. App Library lists catalog dynamically + paints Geometry cover by subject + routes format==html→HtmlBookReader, so the book appears **after backend deploy** (APK rebuild still needed for the new reader: file://+url_launcher+interactive-skip + prior font/paged/zoom fixes). Preview = open the .html in any desktop browser. **NEXT (after founder approves format): Algebra (incl. Identities+Inequalities), Combinatorics, Number Theory** — same pipeline (re-run render+assemble per pillar). Ship: backend `./deploy.sh` + `cd app && flutter pub get && flutter build apk --release -t lib/main_v3.dart`.
- **IOQM 4 PILLARS COMPLETE + STORE MVP (purchase→download→Downloads) 2026-06-20, needs build+device QA**: founder asks — remove dummy books; push real books to store; purchase-before-download; MVP price=0 (free) so trial users can "buy"; only downloaded books show in Downloads; fix Geometry top links; finish the other 3 pillars as separate books. **All 4 pillar books built** (same faithful-WebP-1.7x + interactive-shell pipeline, `outputs/ioqm_build/build_all.py`, idempotent): geometry-ioqm (13 topics·165pg·12.57MB), algebra-ioqm (14 topics incl. Identities+Inequalities·122pg·8.88MB), combinatorics-ioqm (8·68pg·6.55MB), numbertheory-ioqm (8·63pg·6.14MB) → `content-books/{id}/`. **Geometry top-link fix**: in-page Contents links did a WebView reload (file:// nav) → added book JS that intercepts `a[href^="#"]` → `preventDefault + scrollIntoView({smooth})` (in all 4 books). Cover figure is the exact circumcircle+incircle config; "Four Pillars" removed from cover/endcap. **Backend store MVP** (`store_service.py`): **catalog trimmed 13→5** (Euclid's Garden + 4 pillars; ALL dummies + coming-soon removed), every book now `pricing:{isFree:true}` → **nothing auto-owned** (`_AUTO_OWNED` empty) so a book must be **claimed (purchased) before download**; content endpoints already gate `is_owned(uid)` → 403 before purchase, 200 after. `_BOOK_FILES` +3 pillars. **smoke_store rewritten → 20/20** (5 free books, fresh user owns nothing, content 403-before / 200-after-claim, all 4 pillars purchasable+download, auth 403s). **App** (`books_browse.dart` + `html_book_reader.dart`): new `BookDownloads` (saves entitlement-gated bytes to `appDocs/kiwi_books/{id}.html`, atomic .part rename, `downloadedIds`/`localPath`/`remove`); flow = tap **Get·Free** (claim) → **Download** (spinner) → **Read**; tab "My books"→**Downloads** (only downloaded, with count + SAVED ribbon); reader gained `localPath` param → loads the downloaded file **offline** (no fetch; `_ownsTemp` guards temp-cleanup so a download is never deleted). pubspec += `path_provider` use (already dep). Dead `books_integration.dart` dummy ids never surface (Library = BooksBrowseBody on backend catalog). Both Dart files delimiter-balanced. Ship: backend `./deploy.sh` (ships content-books + new catalog → books appear in Store) + APK `cd app && flutter pub get && flutter build apk --release -t lib/main_v3.dart`. **Device QA**: Store shows 5 books → Get→Download→Read; Downloads tab fills only after download; reader opens offline; Geometry Contents links jump (no reload). Phase-2 (founder said later): real prices (swap isFree→coins), per-book download progress %, delete-download, lazy backend image serving if 12MB load feels heavy.
- **NUMBER SENSE interactive workbook — structure done, interactivity in progress (2026-06-21)**: founder uploaded `VEL Wavebook PDFs.zip` → "Number Sense and Operations" folder = **13 worksheet PDFs, 784 slides total, ~198 interactive**. Ask: make ONE interactive book named "Number Sense" from ALL slides; tap-the-answer (e.g. slides 4-5 of the Addition deck) + type-integers-and-check (slides 8-11); founder chose **one ~30MB book** + **all interactive** (spot-checks answers). **Genuine interactivity** (different from IOQM faithful-reveal): keep each slide's faithful VISUAL (cropped above the printed answer area) + replace the answer area with real controls — tap pills (✓ green/✗ shake) or number inputs + Check (per-box green/red). Pipeline in `outputs/nsbuild/`: `render_cache.py` (all 784 slides→webp cache 1.3x, 29.4MB; **run SINGLE-process — duplicate procs caused memory hangs/OOM-137**) + `assemble_ns.py` (config-driven: `INTERACTIVE[(chapter,slide)]`=mcq/fill+answer; faithful slides downscaled 0.82×+q72 method=4 at assembly to keep file light; cover + 13-chapter contents + contents-link smooth-scroll fix + IntersectionObserver lazy images + pick/check JS). Book `content-books/number-sense/number-sense.html` = **26.4MB, 13 chapters, 784 slides, 83 interactive (4 of 13 worksheets done, ONGOING)**. Founder directive: batch per-PDF, QA each, keep going till all done. **DONE worksheets** (chapter idx): c06 Addition (30), c07 Subtraction (30), c08 Challenges A&S≤10 (14), c09 Addition Facts≤20 (9). Interaction types in `assemble_ns.py`: `mcq` (tap, numeric or string opts, optional crop+label), `fill` (a+b=c 3-box, cropped), `fill_sub` (a−b=c, full slide), `one` (single answer, full slide), `multi` (tap-all-correct). Config = `INTERACTIVE[(chapter_idx, slide_1based)]`. Method: per worksheet, pdftotext for word-problems/number-sentences (text-derivable), montage-render + view for counts/number-lines. **Hard slides left FAITHFUL + flagged** (better than wrong-keyed): pick&drop manipulatives, ambiguous object-counts, which-picture/number-line MCQs, multi-blank sign/number drag. **REMAINING worksheets**: c00 Counting, c01 Comparison≤10, c02 Comparison&Seq≤20, c03 Skip-count, c04 Group&Count, c05 Place-value, c10 Add&Sub≤20, c11 Challenges-Add≤20, c12 Challenges-Sub≤20. Re-run `assemble_ns.py` after each (downscale 0.82 method=4, ~40s). Founder to spot-check keyed answers. Wired store: `_BOOK_FILES`+catalog row (free, subject "Number Sense", levels [1], gradeBand K–2) → **catalog 5→6**; smoke_store updated → all PASS; content serves 25.7MB w/ iq+chapters+check JS. **Interactivity runs in the existing HtmlBookReader** (WebView runs the tap/type/check JS) — no app change needed; ship = backend `./deploy.sh` (book appears in Library) + APK for the earlier reader fixes. **REMAINING (big, ongoing): key the other ~192 interactive slides** across all 13 worksheets (count objects / read number-lines / word-problems → mcq/fill/drag config in `assemble_ns.py`, re-run; founder spot-checks). Types still to add: pick-&-drop (drag), number-line complete, word-problem totals, make-10. Standalone demo `outputs/addition_interactive_demo.html` (6 slides) validated the pattern. **Note**: base64 inflates webp ~1.33× (29.4MB cache→would be 39.5MB book; downscale brought it to 25.7MB). Excluded the stray `Angle Sum Properties of Triangle.pdf` (not number-sense).
- **COMPLETE SYSTEM QA done 2026-06-19** (`KIWIMATH_SYSTEM_QA_AND_TECHWORK.md` — master status tying content+adaptive+leaderboard+store+economy + all remaining tech work). Full green snapshot: v3 17/17 · adaptive 18/18 · contest 23/23 · store **19/19** · scanner 0 · pre_deploy · 276 routes · all Flutter balanced. Independent money-code review found+**FIXED a real bug**: `/v3/economy/spend` price gate didn't pin currency → a 300-COIN book could be paid with 300 GEMS; now unlock_book requires currency=='coins' (locked w/ test). Also: failed spends no longer cached (retry-after-earn works), ownership only for real catalog books, per-user in-instance lock on spend. **Accepted limitation:** coin debit+idempotency not yet a Firestore TRANSACTION (cross-instance double-tap could double-debit; virtual currency + money stubbed + client in-flight guard → nil risk now; **MUST become a txn before real-money/IAP**). Remaining tech work prioritized in the doc: A) deploy/cron/APK now · B) before-real-money (spend txn, IAP, O2/O3) · C) features (content ingestion=Phase1.5 ready for the book folder, sync, rating/seasons V2, league→book-gift flywheel, one-Gems consolidation, app skill/league counters) · D) accepted last-write-wins + ladder version stamp.

## Me
Anand Prakash (anand.prakash@vedantu.com), Founder of Kiwimath — adaptive math learning app for K-6 kids. Building with Flutter + FastAPI + Firebase.

## Current Status (May 5, 2026)

**v8 APP: 6-tab nav (Practice/Worksheets/School/Clan/Growth/Parent), 28,803+ questions, all systems live**

### What's Built & Verified:
- ✅ **v8 Flutter App**: 6-tab nav — Practice (smart adaptive), Worksheets (100 daily olympiad), School (multi-curriculum), Clan (social+puzzles), Growth (mountain journey), Parent (PIN-protected)
- ✅ **Practice tab**: Smart adaptive practice with topic unlocking, 8 core topics per grade
- ✅ **Worksheets tab**: 600 olympiad worksheets (100/grade × 6 grades), 3 views (Topics/Journey/Grid), per-worksheet + bulk download, offline cache
- ✅ **School tab**: Multi-curriculum chapter browser (Cambridge, NCERT, Singapore, ICSE, US Common Core sub-tabs)
- ✅ **Clan tab**: Daily puzzle + streak system, clan wars, engagement rewards, join/create clan
- ✅ **Growth tab**: Mountain journey visualization, milestones, scale scores, proficiency levels
- ✅ **Parent tab**: PIN-gated dashboard with diagnostic reports, clan section
- ✅ **Terry Chew "why" explanations**: All 21,603 practice questions + 7,200 olympiad questions have Analysis-style explanations
- ✅ **Olympiad worksheet system**: 30 batch JSON files, SVG visual renderer, mixed interaction modes (MCQ, integer, fill-up, drag-drop, match-column)
- ✅ Vedantu LO System: competency taxonomy (K/A/R), 6-level proficiency, scale scores, growth tracking, benchmark tests, auto-remedial, parent diagnostic reports
- ✅ Adaptive engine (5 phases) + Flutter wired to `/v2/session/unified`
- ✅ Content v4: 22,467 base questions → 36,459 grade-topic organized (multi-grade overlap via KiwiTier)
- ✅ Clan system: 14 clan endpoints + engagement (daily puzzle, streak, leagues, wars, rewards)

### Content v4 Summary:
| Grade | Topics | Questions |
|-------|--------|-----------|
| 1 | 8 | 4,301 |
| 2 | 8 | 6,674 |
| 3 | 11 | 7,437 |
| 4 | 11 | 7,055 |
| 5 | 10 | 5,104 |
| 6 | 9 | 5,888 |
| **Total** | **57** | **36,459** |

### KiwiTier Level→Grade Mapping:
```
Level 1 → Grade 1 + Grade 2
Level 2 → Grade 2 + Grade 3
Level 3 → Grade 3 + Grade 4
Level 4 → Grade 4 + Grade 5
Grade 6 ← cloned from Level 3-4 with +0.3 IRT bump
```

### Content v4 File Structure:
```
content-v4/
  adaptive/grade{1-6}/{topic-id}.json  — IRT-sequenced questions per topic
  adaptive/grade{1-6}/index.json       — grade-level topic index
  school/{curriculum}/grade{1-6}/chapters.json — chapter references
  locale_config.json                   — shared locale data (india/singapore/us)
  topic_map.json                       — master topic definitions
  visual_coverage_report.json          — visual audit results
```

### Quality Fixes Applied (schema 4.0 → 4.1):
- Domain misclassifications fixed (algebra, data topics)
- 2,883+ identical diagnostics deduplicated
- 7,978 hint spoilers remediated
- country_context extracted to shared locale_config.json (160MB → 100.5MB)
- 23,194 null school_grade values populated
- 43 cross-operation diagnostic errors fixed
- 433 pure arithmetic questions moved out of word-problem files
- g5-percent rebuilt from 5 rounding stubs → 120 proper percentage/ratio questions
- G3/G4 data_handling separated from measurement into dedicated topics
- G3 multiplication (215→400) and division (178→398) bolstered
- Visual requirements audited: 9,425 over-tagged essentials downgraded, 6,686 fake SVG refs cleared
- 15 genuinely essential questions got inline SVGs generated
- Final: 636 essential (all covered), 22,090 optional, 13,733 none

### Flutter v8 Navigation:
```
Tab 0: Practice    → OlympiadScreen (Smart adaptive practice, topic unlocking G1-G6)
Tab 1: Worksheets  → OlympiadTabScreen (sub-tabs: Smart Practice / Daily Worksheets / Downloads)
Tab 2: School      → CurriculumScreen (Cambridge/NCERT/Singapore/ICSE/USCC sub-tabs)
Tab 3: Clan        → ClanHubScreen (daily puzzle, streak, wars, rewards) or ClanLanding (join/create)
Tab 4: Growth      → GrowthScreen (mountain journey, milestones, proficiency)
Tab 5: Parent      → ParentDashboardScreen (PIN-gated via PinGate)
```
- PIN gate: `app/lib/widgets/pin_gate.dart` — 4-digit numeric PIN, SharedPreferences
- Bottom nav: `_AppShell` in `app/lib/main.dart` (v8) — 6 tabs: Olympiad (star icon, orange), School (house), Growth (chart), Clan (people group), Parent (person+child), Profile (person)
- App icon: minimal white kiwi bird + sparkle on orange gradient

### Olympiad Worksheet System (BUILT):
- **600 worksheets**: 100 per grade × 6 grades, stored in `content/olympiad/g{1-6}_olympiad_batch{1-5}.json`
- **12 questions each**: Mixed difficulty (warmup/practice/challenge), mixed interaction modes
- **5 interaction modes**: MCQ, integer, fill_up, drag_drop, match_column
- **SVG visuals**: Parameterized components in `content/olympiad/svg_components/`, rendered on-demand via `/olympiad/questions/{id}/visual`
- **Titles & topics**: Each worksheet has engaging title (e.g., "Pattern Detective", "Number Ninja"), subtitle (topic chips), dominant_topic
- **Terry Chew Analysis**: All 7,200 question approaches normalized to "Analysis: [method]... Answer: [correct]." format
- **Offline cache**: `WorksheetCache` — per-worksheet + bulk download, disk persistence, SVG pre-fetch
- **3-view UI**: Topic-grouped (default), Journey (swipeable cards), Grid (10×10 calendar)
- **API**: GET `/olympiad/worksheets?grade=N&day=D`, GET `/olympiad/worksheets/list?grade=N` (rich metadata), GET `/olympiad/questions/{qid}/visual`, GET `/olympiad/stats`

### Practice Bank "Why" Explanations (BUILT):
- **21,603 practice questions** have `w` field with Terry Chew-style Analysis explanations
- **Format**: "Analysis: [method/insight].\n[step-by-step reasoning with actual math].\nAnswer: [correct answer]."
- **Topic-specific generators**: pattern_sequence, word_problem, logic, spatial, shapes, fractions, measurement, data_handling, algebra, ratio_proportion, etc.
- **Zero fallbacks**: All questions have specific, math-aware explanations (not generic)
- **File**: `backend/static/all_questions.json` — compact format with keys: id, s, c, a, d, k, t, g, src, svg, w

## Key Architecture:
| Component | File | Purpose |
|-----------|------|---------|
| Content Store v4 | `content_store_v4.py` | Grade-topic structured content, adaptive selection |
| Content Store v2 | `content_store_v2.py` | QuestionV2 pydantic model (50+ fields) |
| v4 API Router | `api/questions_v4.py` | 14 endpoints: topics, adaptive, school, offline, locking |
| Session Lock | `session_lock.py` | Multi-device session locking with TTL + heartbeat |
| Skill Mapper | `skill_mapper.py` | Maps questions → 37 skill nodes |
| Unified Planner | `unified_session_planner.py` | Cross-curriculum adaptive sessions + Welcome Back mode |
| Path Engine | `path_engine.py` | 37-node prerequisite graph + learning paths |
| IRT Model | `irt_model.py` | 3PL with EAP estimation |

## Terms
| Term | Meaning |
|------|---------|
| **KiwiTier** | Level system: Junior (G1-2) + Senior (G3-6), each Level spans 2 grades |
| **dual-tagging** | Curriculum questions tagged for both adaptive topics AND school chapters |
| **IRT-b** | Item difficulty parameter; questions sorted ascending within each topic |
| **theta (θ)** | Student ability estimate on IRT scale (-3 to +3) |
| **essential visual** | Question cannot be answered without seeing the SVG/image |
| **smart practice** | Unified session pulling from all curricula adaptively |
| **the moat** | Content quality + explanation quality + parent trust + child delight |
| **AH** | Academic Height — 3D mastery: Depth + Breadth + Stability |
| **ZPD** | Zone of Proximal Development — P(correct) ∈ [0.60, 0.85] |
| **3-R Framework** | "Why?" diagnostics: Re-Contextualize → Redirect → Reinforce |
| **Crunch Mode** | Weeks 31-40: retrieval practice, no new levels, exam-ready |
| **K/A/R** | TIMSS competency taxonomy: Knowing, Applying, Reasoning |
| **Scale Score** | IRT theta → 200-800 scale (mean=500, SD=50) for parent-friendly reporting |
| **Proficiency Level** | L1-L6 named levels (Explorer→Legend), mapped from theta ranges |
| **Benchmark Test** | 20-question structured diagnostic with anchor items for equating |
| **Auto-Remedial** | K-wrong → insert 3 easier same-concept questions (max 2/session) |

## Clan System (BUILT — Social/Engagement Layer)
| Term | Meaning |
|------|---------|
| **Clan** | Student-created group, invite-only, grade-locked, max 15 members |
| **Brain Points** | Clan points from members' challenge scores (top 10 count) |
| **Brawn Points** | Bonus points: 50 × active members |
| **Quiz Points** | Weighted mean of daily quiz scores × active count × 10 |
| **Full Squad Bonus** | 2× Brain Points if ALL members practice same day |
| **Picture Unravel** | Clan-exclusive challenge: earn points → reveal olympiad-level PUZZLE image, group solves it |
| **Guess Board** | 1 guess/day per member, 60 chars max, profanity-filtered — collaborative puzzle-solving workspace |
| **Challenge Bust** | When a challenge expires/completes and points refresh |
| **Clan Leader** | Creator of clan, only one who can submit Picture Unravel answers |
| **Clan Levels** | Seedling (0) → Sapling (5K) → Tree (15K) → Forest (40K) → Ancient Grove (100K) XP |

### Clan Architecture:
| Component | File | Purpose |
|-----------|------|---------|
| Clan Service | `backend/app/services/clan_service.py` | Core logic: scoring, levels, filtering, invites |
| Clan Firestore | `backend/app/services/clan_firestore.py` | Firestore persistence layer (14 methods) |
| Clan API | `backend/app/api/clans.py` | 14 REST endpoints under /v4 |
| Clan Models | `app/lib/models/clan.dart` | 8 Dart data models |
| Clan Service (Flutter) | `app/lib/services/clan_service.dart` | Flutter API client (13 methods) |
| Clan Hub | `app/lib/screens/clan_hub_screen.dart` | Main clan view |
| Create/Join | `app/lib/screens/clan_create_screen.dart`, `clan_join_screen.dart` | Parent-gated flows |
| Challenge | `app/lib/screens/picture_challenge_screen.dart` | Pixel grid + guess board |
| Leaderboard | `app/lib/screens/clan_leaderboard_screen.dart` | Grade-scoped top 20 |
| Widgets | `app/lib/widgets/clan_crest_widget.dart`, `pixel_grid_widget.dart`, `squad_activity_bar.dart`, `guess_board_widget.dart` | Reusable clan UI components |
| Puzzles | `backend/static/puzzles/` | 6 SVG puzzles (Star Map, Locked Grid, Code Breaker, River Crossing, Spiral Tower, Einstein's Garden) |
| Daily Cron | `backend/deploy/clan_cron.yaml` | Cloud Scheduler for midnight IST aggregation |

→ Full details: memory/projects/clan-construct.md

### Engagement System (BUILT):
| Component | File | Purpose |
|-----------|------|---------|
| Daily Puzzle API | `backend/app/api/engagement.py` | Daily puzzle endpoints (get, submit, streak) |
| League API | `backend/app/api/engagement.py` | League leaderboards, clan wars |
| Rewards API | `backend/app/api/engagement.py` | Achievement unlocks, XP tracking |
| Puzzle Screen | `app/lib/screens/daily_puzzle_screen.dart` | Daily puzzle solve flow |
| Streak Screen | `app/lib/screens/streak_screen.dart` | Streak calendar + rewards |
| Wars Screen | `app/lib/screens/clan_wars_screen.dart` | Clan vs clan battles |
| Rewards Screen | `app/lib/screens/rewards_screen.dart` | Achievements + XP store |
| Growth Screen | `app/lib/screens/growth_screen.dart` | Mountain journey + milestones |
| Growth API | `backend/app/api/growth.py` | Growth endpoints, milestone detection |

### Daily Puzzle System (BUILT)
| Term | Meaning |
|------|---------|
| **Daily Puzzle** | One new puzzle per grade per day, drops 4 PM IST (after school), closes 10 PM IST |
| **Narrative Style** | Story-wrapped puzzles from 9 books (~2,000 puzzles analyzed). 5 styles: Smullyan Dialogue Logic, Moscow Real-World, Everything Kids Visual, Yoshigahara Spatial, Party Trick/Game |
| **Puzzle Books Deep-Read** | Alice in Puzzle-Land, Lady or Tiger, What is the Name of This Book, To Mock a Mockingbird, This Book Needs No Title, Riddle of Scheherazade, Godelian Puzzle Book (all Smullyan) + Moscow Puzzles (Kordemsky) + Puzzles of Yoshigahara |
→ Full analysis: memory/puzzle-books/ (4 files: smullyan-storytelling-patterns.md, moscow-puzzles-patterns.md, yoshigahara-visual-puzzles.md, puzzle-creation-guide.md) |
| **IPS** | Individual Puzzle Score: Accuracy(50%) + Speed(30%) + Streak(15%) + Bonus(5%) = max 1,000 |
| **CDS** | Clan Daily Score: Brain(top 10 IPS × participation multiplier) + Brawn(solvers×50) + Full Squad(+500) |
| **Streak** | Consecutive daily solves: Day 1=10 → Day 7+=150 pts. Missing a day resets. |
| **Puzzle Types** | 10 types: Number Crunch, Pattern Hunt, Shape Shift, Logic Lock, Measure Up, Code Break, Speed Race, Coin & Dice, KenKen Grid, Story Puzzle |
| **Leagues** | Bronze → Silver → Gold → Diamond → Legendary (trophy-based, seasonal reset) |
| **Special Modifiers** | Double XP (10%), Boss Puzzle (10%), Mystery (5%), Clan vs Clan Duel (5%) |
| **Build Plan** | Phase 1: 60 puzzles/grade MVP → Phase 2: Social layer → Phase 3: Leagues → Phase 4: 360/grade/year |
→ Design doc: Kiwimath_Clan_Daily_Puzzle_System.docx

## Vedantu Learning Outcomes System (BUILT — Assessment & Reporting Layer)
| Term | Meaning |
|------|---------|
| **K/A/R** | Competency taxonomy: Knowing (recall), Applying (use/solve), Reasoning (analyze/justify) — TIMSS-aligned |
| **Scale Score** | Student ability on 200-800 scale (mean=500, SD=50), transformed from IRT theta |
| **Proficiency Levels** | L1 Explorer → L2 Builder → L3 Achiever → L4 Star → L5 Champion → L6 Legend |
| **Benchmark Test** | 20-question structured diagnostic with anchor items for score equating |
| **MLE Theta** | Newton-Raphson 3PL estimation for scoring benchmarks (more accurate than practice ELO) |
| **Auto-Remedial** | When K-tagged question wrong → insert 3 easier same-concept questions |
| **Growth Tracking** | GrowthSnapshot records over time, trajectory: improving/steady/declining |

### LO Architecture:
| Component | File | Purpose |
|-----------|------|---------|
| Competency Tagger | `backend/scripts/competency_tagger.py` | Auto-classifies 26,722 questions as K/A/R |
| Proficiency Levels | `backend/app/services/proficiency_levels.py` | 6-level system, scale scores, CompetencyProfile, GrowthSnapshot |
| Remedial Engine | `backend/app/services/remedial_engine.py` | Auto-remedial: concept groups, trigger logic, question selection |
| Benchmark Service | `backend/app/services/benchmark_test.py` | Structured tests, anchor items, MLE scoring |
| Proficiency Card | `app/lib/widgets/proficiency_card.dart` | Flutter widget: level badge, scale score, K/A/R bars, growth |
| Benchmark Screen | `app/lib/screens/benchmark_test_screen.dart` | Flutter: 20-question diagnostic flow with results |

### LO API Endpoints:
```
GET  /v2/proficiency         — student's level, scale score, competency breakdown
GET  /v2/proficiency/levels  — all 6 level definitions
POST /v2/benchmark/create    — create structured diagnostic test
POST /v2/benchmark/submit    — submit and score benchmark responses
GET  /v2/benchmark/history   — benchmark history with growth comparison
GET  /v2/remedial/stats      — remedial effectiveness analytics
```

### Competency Distribution (26,722 questions):
K=11,766 (44%), A=12,542 (47%), R=2,414 (9%)

### Clan API Contract (Dart → Python field mapping):
```
POST /v4/clans:        leader_uid, parent_uid, name, grade, crest_shape, crest_color
POST /v4/clans/join:   invite_code, uid, parent_uid, grade
POST /v4/clans/{id}/react:  uid, emoji
POST /v4/challenges/{id}/answer:  clan_id, uid, answer
POST /v4/challenges/{id}/guess:   clan_id, uid, guess_text
Crest shapes: bolt, lion, wave, rocket, blossom, dolphin
parent_uid = same as userId (parent gate is 4-digit PIN in UI)
```

### Clan Local Test Results (May 3, 2026):
All 14 endpoints passing: create, get, mine, join, invite regen, remove member, leaderboard, react, active challenge, progress, submit guess, get guesses, submit answer, daily aggregate

### Bug Fixes (May 3, 2026):
- **tap_to_reveal "no option" bug FIXED**: Two bugs — (1) `questions_v2.py` `_to_response()` stripped choices for non-mcq modes, fixed to send choices whenever they exist; (2) 1,555 questions had unimplemented `tap_to_reveal` mode across 11 content-v2 files, all changed to `mcq`
- **Color(KiwiColors.xxx) compile errors FIXED**: 4 widget files had `Color(KiwiColors.xxx)` wrapping already-typed Color values; removed redundant Color() constructors

### Benchmark Audit & Fixes (May 4, 2026):
- **CRITICAL: Grade filtering added to benchmark tests**: `benchmark_test.py` now filters questions by grade before selection — Grade N test only gets Grade N-1 + N content. Previously used ALL 26,722 questions regardless of grade.
- **8 wrong ICSE-G3 comparison answers FIXED**: Choices contained neither of the two numbers being compared. All 8 in `icse_g3_questions.json`.
- **2 competency mistagged questions FIXED**: T4-0905, T4-0915 (logical negation) changed K → R
- **26 placeholder rect-only SVGs cleared**: ICSE-G1 (12) + USCC-G1/G3 (14) had empty rectangles
- **3,600 curriculum questions got topic tags**: Singapore, USCC, ICSE questions had empty topic field, now auto-assigned based on chapter/tags/stem
- **Full audit report**: `BENCHMARK_AUDIT_REPORT.md`

### Benjamin Olympiad G6 Content (May 4, 2026):
- **17 Benjamin PDFs extracted**: 2009-2025, 408 questions total (24 per year)
- **2009-2012 manually extracted** from visual PDF reading (OCR failed on 2009-2011, 2012 had artifacts)
- **379 original questions** built in content-v2 format with IRT parameters and competency tags
- **505 variant questions** generated: 202 step-down + 202 step-up + 101 similar
- **G6 Smart Practice enabled**: QA review server now serves Benjamin questions for Grade 6
- **Content location**: `content-v2/benjamin-olympiad/grade6/`
- **216 filler sentences removed**: "workspace" intros, disconnected character names stripped from stems across 11 files

### Story Rewriting System (May 4, 2026):
- **All 12,185 Kangaroo questions rewritten** with continuous story narratives
- **16 story themes** across 8 topics × 2 grade bands (junior G1-3 + senior G4-6)
- **Characters G1-3**: Kiwi (lead), Chikoo, Aarohi, Vanya, Riya
- **Characters G4-6**: Kiwi (lead), Ved, Nuha, Google, Veronica
- **Story themes**: The Enchanted Garden, The LEGO Kingdom, The Music Festival, The Treasure Hunt, The Pirate Ship, The Shape Village, The Forest Bakery, The Wizard's Tower (junior) | The Lost City Expedition, The Racing Garage, The Code Breakers, The Demon Hunters, The Space Station, The Architect's Challenge, The World Tour, The Escape Room (senior)
- **8 chapters per topic** — questions progress through the story arc
- **Zero junk remaining**: all old character names (Knight Koko, Chef Cheetah, etc.), workspace mentions, filler sentences stripped
- **Math preserved**: all numbers, operations, choices, and correct answers unchanged
- **Pipeline**: `content-v2/_workspace/rewrite_stories.py` — re-runnable, idempotent
- **Story design**: `content-v2/_workspace/story_design.md`

### Visual & Quality Audit (May 4, 2026):
- **111 visual_context mismatches fixed**: ctx said "pictograph" but stem said "bar graph", wrong clock/number line refs
- **262 empty math stems reconstructed**: 75 from original_stem, 187 from tags/choices
- **74 old character names stripped**: Professor Panda, Ninja Nemo, Astronaut Ava, etc.
- **138 fragment leftovers cleaned**: "Help calculate:", "Needs to work out:", "Needs the GCD!"

### Deduplication (May 4, 2026):
- **12,185 → 8,902 questions** after removing duplicates and near-duplicates
- **342 exact duplicates removed**: same stem + same choices + same answer
- **1,147 near duplicates removed**: same stem, different distractors, same answer
- **1,790 skeleton overloads trimmed**: 4+ questions with identical template in same file (kept max 8 per group)
- **18 duplicate IDs fixed**: T2-1001–T2-1020 clashed between g56 and grade34_variety files
- **Final distribution**: G1-2: 5,387 | G3-4: 1,079 | G5-6: 2,373 | Other: 63
- **All 8,902 IDs verified unique**

### Curriculum Deduplication (May 4, 2026):
- **Singapore**: 1,200 → 1,023 (-177) — number bonds, fraction bars, angle type dupes
  - **+325 SMC questions added** (from 2023 Singapore Math Challenge PDFs G1-G6): fills 104 gap topics (money, time, logic, algebra, patterns, combinatorics, etc.) → **1,348 total Singapore questions**
- **IGCSE (Cambridge Primary)**: 3,600 → 2,808 (-792) — place value, triangle angles, shape sides dupes
- **NCERT**: 3,159 → 2,734 (-425) — G3-G6 had heavy skeleton overload
- **ICSE**: 2,372 → 1,835 (-537) — G1 had 154 dupes alone
- **US Common Core**: 1,200 → 990 (-210) — G1-G3 most affected
- **All 6 curricula verified clean**: 0 dup IDs, 0 exact dupes, 0 skeleton overload
- **Grand total across all content**: 18,617 unique questions (18,292 + 325 SMC)

### Wavebook Question Bank (BUILT — May 5, 2026):
- **551 questions** from Vedantu live olympiad class worksheets (separate from main olympiad system)
- **Level 3** (Grades 3-4): 239 questions, 21 topics, 4 batches — Numbers, Multiplication, Factors, Area, Perimeter, Symmetry, Venn Diagrams, Patterns, Clocks, Data Handling
- **Level 4** (Grades 5-6): 312 questions, 26 topics, 4 batches — Numbers, Divisibility, Factors, Percentage, Profit/Loss, Ratio/Proportion, Quadrilaterals/Polygons
- **25 SVG illustrations**: Geometry figures, Venn diagrams, bar charts, clock faces, symmetry shapes, area grids, number patterns
- **Content location**: `content-v2/wavebook/` — 8 batch JSONs + `svg/` subfolder + `memory/wavebook-index.md`
- **Source**: 18 Google Drive PDFs (file IDs in `_meta.json`)
- **Backend**: `backend/app/api/wavebook.py` — 4 endpoints (/wavebook/topics, /questions, /download, /stats)
- **Flutter**: `app/lib/screens/wavebook_screen.dart` — full MCQ solve flow with per-topic download

### UI Redesign (May 5, 2026) — Worksheet Tab Addition:
- **Bottom nav remains 6 tabs**: Olympiad (star, orange active), School (house), Growth (chart), Clan (people group), Parent (person+child), Profile (person)
- **DPP tab** (formerly "Worksheets"): Daily Practice Problems — olympiad worksheets, all grades, Topics|Grid toggle, Journey removed, play bar removed, downloadable per-worksheet
- **Worksheet tab**: NEW tab added in top shelf alongside DPP — only for G3+, shows live class wavebook MCQs
  - G3/G4 share Level 3 content, G5/G6 share Level 4 content
  - Downloadable individually per topic
  - Topics|Grid toggle (same as DPP)
  - No Journey toggle, no play bar, no "New" badge, no numeric counts (question counts, worksheet counts, completion stats)
- **Top shelf sub-tabs** (within the DPP bottom nav tab): Practice | DPP | Worksheet | Saved
- **Kiwimath Orange**: #FF6F00 (primary accent throughout)

### Currency System (Audited May 5, 2026):
- **Kiwi Coins**: Effort-based, spendable in Shop (avatar items). Earned on every practice session.
- **Mastery Gems**: Skill-based, achievement-gated legendary items. Harder to earn.
- **totalGems**: Engagement gems from daily calendar (7-day cycle: day1=10, day3=25, day5=50, day7=100) + mystery boxes. Separate from Mastery Gems.
- **XP**: Earned per question answered. Drives level system.
- **Profile screen shows**: XP, totalGems, Streak, Topics Mastered, Kiwi Coins, Mastery Gems (6 stats in 3x2 grid — FIXED May 5)
- **⚠️ "gems" exists in TWO backend systems**: gamification (state.gems = Mastery Gems) AND engagement (total_gems = calendar rewards). Profile now shows both separately.
- **Parent dashboard deliberately hides all currencies** — shows only mastery/accuracy/strengths.
- **Clan Hub is the reward HUB** — daily calendar, mystery boxes, stickers, leagues all claimed here.
- **Wavebook/Worksheet**: No client-side reward logic — rewards granted server-side on answer submit (same as Practice/DPP).

### Pending:
- **⚠️ CRITICAL: Git commit needed** — Worksheet tab files are UNTRACKED: olympiad_tab_screen.dart, wavebook_screen.dart, worksheet_list_screen.dart, wavebook.py, content-v2/wavebook/. Anand must `git add` + commit + push from Terminal.
- **Deploy**: Backend + Flutter need redeployment (worksheet tab, gems fix, "why" explanations, engagement system)
- **Benjamin answers need verification**: correct_answer currently set to first choice as placeholder
- **Benjamin visual questions**: 175 questions need SVG generation from PDF images
- **Benjamin variants**: choices need recalculation (inherited from originals, not recomputed)
- **FlagStore → Firestore**: flag_store.py is in-memory only, flags lost on deploy/restart. Need to wire to Firestore.
- **Git push**: .git/index.lock blocks sandbox — Anand needs to run `git add -A && git commit && git push` from Terminal

## Engagement + onboarding work (2026-06-25)
- **Live usage dashboard**: new key-gated `GET /v3/admin/usage` (`backend/app/api/usage.py`, wired in `main.py`) — `auth.list_users()` for the true headcount + per-user enrichment from `users/{uid}` profile + `gamification/state` (questions/accuracy/sessions/streak/xp/topics); aggregates totals, active windows, new-by-day, grade/topic dist. CORS-open + a same-origin dashboard at `GET /v3/dashboard?key=` (no CORS). Default key `kmx-founder-7Q2v9Lp4Ad` (override via `KIWIMATH_ADMIN_KEY`). Standalone `kiwimath_dashboard.html` + `KIWIMATH_DASHBOARD_HOWTO.md`. **Live read: 17 users, 12 active/wk, 1,134 Qs @ 78.7%.** The old `/admin/analytics/overview` undercounts (in-memory only).
- **Daily-streak bug FIXED** (data showed streak=0 for ALL users incl. 2-month founder): the live `/v3/answer/check` path (`gamification.record_answer`) only tracked in-session `consecutive_correct`, never the **daily** streak. Added `streak_last_date` + `_ist_date()` + `live_streak()` to `GamificationState`; record_answer now extends/resets the daily streak (IST day boundary) on the first answer each day, tracks longest, emits `streak_day`. `get_profile_summary` + `me_progress` show the **honest** streak (0 if last-active < yesterday). Unit-tested (extend→reset→honest all pass), smoke 17/17. Ships backend-only (app already reads streak). Existing users start fresh (no backfill).
- **LEVEL-SCOPED app + onboarding** (founder: "app behaves per onboarding — pick L1 → whole app shows only that level; change in profile; right now overwhelming"). UX decision: *focused, not caged* — one level's world at a time + an always-visible header level chip + Profile to switch; onboarding is **grade-first** (parent knows grade → auto-recommend the matching level with its exams; also captures grade, fixing 14/17-no-grade). **Backend**: `LEVEL_META` (grades/exams/tagline/emoji/grade_min/max) enriches `/v3/olympiad/levels`; `selected_level` on the profile + `GET/POST /v3/me/settings`. **Flutter**: new `v3/level_onboarding.dart` (grade-first chooser + `LevelCard` + `showLevelChooser` sheet + `saveSelectedLevel` prefs+backend); shell (`kiwi_v3.dart`) loads `selected_level` (prefs→backend), shows onboarding if none, header level chip, passes level to tabs; **Olympiad** opens straight into the level's topics (no L1-L8 grid); **School** defaults grade to the level's band; **Library** (`books_browse.dart`) opens pre-filtered to the level (`lockedLevel`); **Progress** scoped to level; **Profile** has a Change-level tile. Backend verified (smoke 17/17, pre_deploy green); Flutter structural-checked (all delimiters balanced, 0 dangling refs) — **needs APK build + device QA**. Ship: backend `./deploy.sh` + `flutter build apk --release -t lib/main_v3.dart`.

## Critical Product Principles:
- **IRON RULE: Grades exist ONLY in the Curriculum tab** — core PLAY uses Levels only
- **The REAL moat is content quality** — engine only adapts BAD content faster
- **Child feels:** "This is fun. I can win." **Parent feels:** "I understand exactly what is happening."
- Never expose engineering complexity (IRT/theta/FSRS) to users
- IGCSE → **Cambridge Primary** (IGCSE is not K-6)
- Kiwimath Orange: Primary #FF6D00, Dark #E65100, Light #FFF3E0

## Deploy Instructions
```bash
cd ~/Downloads/kiwimath/backend && ./deploy.sh   # Backend → Cloud Run (asia-south1)
cd ~/Downloads/kiwimath/app && flutter build apk --release   # Flutter APK
```

## Preferences
- "keep working on it and ill be back in sometime"
- Research-first approach: study papers, plan, then execute
- Don't add unnecessary visuals — only where genuinely required
- Go screen-by-screen sequentially (UI only)
